# 棋盘美化第一轮

## 本轮范围

- 米白纸纹盘面、青绿背景、细木色边与较浅投影。
- 四季浅色玉牌、统一深色格名，保留事件图标与圈层细边。
- 当前格持续显示金色定位边框，并随棋子移动；预览格使用青绿虚线。
- 手机和平板（≤900px）开局收起 HUD，不继承旧的展开偏好；同屏只展开一个面板，掷骰时收起。
- 手机详情浮层与顶部摘要分离，限制滚动区高度，保持切换按钮可用；窄屏仍显示文心数量。
- 320px 与短横屏微调初始缩放下限，保留手势缩放和拖动。
- 新增样式及更新模块缓存版本；未更改玩法、地图配置或中央插画。

## 验证

- `node --test feihuaqi-playable/tests/board-view.test.mjs feihuaqi-playable/tests/board-glyphs.test.mjs`：12 项通过。
- `node feihuaqi-playable/tests/hud-layout.test.mjs`：通过。
- `node --check` 检查 board.js、hud.js：通过。
- 启动 `PORT=8087 node scripts/serve-playable.mjs` 后运行 `node docs/board-polish-20260912/check.mjs`。
- Edge 无头浏览器：1440×900、768×1024、390×844、320×740、844×390。
- 使用当前工作区生产组件与本地配置构造开局状态，移除主菜单/引导遮罩；这组截图属于组件集成验证，不是完整一局通关测试。
- 检查旧展开偏好、摘要收起、面板互斥、详情尺寸、掷骰收起、当前格移动、省电档定位边、页面异常和初始当前位置边界。结果见 results.json。
- 小屏总览的格名依然较小，完整阅读可使用已有缩放操作。

## 备份边界

本地原有未提交及已暂存内容均保留。本轮备份从原 HEAD `f278de93` 提取独立增量，使用独立索引，避免将此前 UI 文案、配置与音乐等改动混入。工作区既有 import map 保留并更新本轮两个模块版本；备份提交仅加入这两个模块需要的缓存映射。

原仓库存在无效旧标签和缺失对象；未覆盖、移动或删除任何既有标签。备份分支与带注释的新标签在交付信息中给出。
