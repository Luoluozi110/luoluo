# UI 排查证据

基线：309547e1b3fc88561656087d7a0d0c82062b0089；日期：2026-09-10。

`hud-{width}.png` 为展开六维面板的截图；`ability-{width}.png` 为修习弹窗首屏。宽度分别为1440、1280、768、390；高度分别为900、720、1024、844。模拟中局数据由正式组件渲染，不代表自然游玩轨迹；后台棋盘来自新开局。

`measurements.json` 保存尺寸和内容测量。`capture.mjs` 在仓库根目录运行：`node docs/ui-audit-20260910/capture.mjs`。脚本使用本机 Playwright 绝对路径和 Edge，迁移机器需调整导入路径。临时浏览器不读取玩家现有存档，云配置请求替换为当前检出配置。

截图支持排版判断，不支持云端可用性或全部游戏交互已验收的结论。
