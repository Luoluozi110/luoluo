# 桃花岛·飞花棋 —— P2 打磨项落地与验证

> 设计师视角：先结论，再论据。每项配 rationale + 仿真背书（脚本 `sim_p2.mjs`，每场景 2000 局，自动最优战斗、答题率 75%）。
> 前置：P0 + P1-2 + P1-1 + P1-3 均已生效；本次 P2 在已校准分布上做打磨，并因分布再位移而**第三次重拟合带宽**。

---

## 结论先行

| 项 | 改动 | 预期 | 实测影响 |
|---|---|---|---|
| **P2.1** 捷才 | `jiecai.turns_max` 34 → **38** | 中位 38 回合的玩家也能拿速度奖励，速通成真实分支 | mean +25，封笔/余灵不变 |
| **P2.2** 题库 d3 | lap2 d3 权重 0.40 → **0.30**（rules.js） | 缓解 lap2 长局 d3 耗尽退化 | 不影响分数；纯内容多样性 |
| **P2.3** 递减软上限 | `diminish` soft 28→**34**、hard 40→**48** | 给专精/堆单属性留头 | mean +32，通胀受 taper+winScale 封顶，p99 几乎不动 |
| **重拟合** | 九档按组合分布再上移 | 文宗拉回精英档 | 文宗 4.7%（原 8%） |

---

## 一、P2.1：捷才（≤34回合）难达成

### 事实
`grades.json` 圆满分 `jiecai`（speed 组，与 `congrong` 互斥 via `else if` 优先级）要求 `turns ≤ 34`。实测**中位 38 回合**，故 ≤34 仅为快于约前 1/4 的玩家可得 +150 速度奖励——对多数玩家是**死内容**。

### 补丁
`jiecai.cond.value` 34 → **38**，desc 同步改「≤38 回合」。与 `congrong`（≤40 且余灵≥5）形成梯度：≤38 捷才(+150) 优先，39–40 且余灵≥5 从容(+100)。

### rationale
让"速通"从摆设变成真实策略分支——玩家可主动选择少走支线/速战速决换速度奖励，增加 build 多样性。中位玩家约半数局可达，符合"中段密集"的设计目标。

### 仿真（隔离）
mean 2631 → **2656 (+25)**；封笔 0.8%→0.8%、余灵 10→10、回合 39→39（不动）。温和、无副作用。

---

## 二、P2.2：题库难度3仅 12 题

### 事实
`questions.json`：**d1 20 / d2 34 / d3 12**（d3 仅 18%）。`difficultyWeights` 硬编码于 `rules.js`：lap2 = {1:0.15, 2:0.45, 3:**0.40**}。`pickQuestion` 在某难度缺题时自动放宽到相邻难度——长 lap2 局会把 12 道 d3 耗尽，难度梯度塌缩为 d2/d1。

### 补丁（立即可调的安全杠杆）
`rules.js` lap2 权重 `{1:0.15, 2:0.45, 3:0.40}` → `{1:0.15, 2:0.55, 3:0.30}`。d3 频率降低、负载转移到充裕的 d2（34 题）。

### rationale 与边界
- 这是**缓解**手段，非根治。根治是**补内容**：把 d3 扩充到 ≥20 题（需由内容负责人按史实/出处严谨出题，超出数值 tuning 范畴，不在本次执行）。
- 不改变任何分数（抽题难度与得分无关），故**无需重拟合**。
- palace 阶段 d3 权重 0.70 维持不变——殿试战斗用题材而非题库，不吃 d3 抽题。

---

## 三、P2.3：diminish 软上限过严（诚实修订）

### 原报告 premise 已被证伪 —— 必须说清
原报告称"诗仙需 shi≥30 在递减区里极难堆"。但仿真实测**专精达成率 68%**（多数玩家本就走门派且能达到单属性 ≥30）。故"让专精可达"这一前提**已不成立**。

### 真实效果（仍按 P2 范围执行，但重新定性）
`diminishGain`：单属性 ≥soft 按 midRate(0.5)、≥hard 按 highRate(0.25) 折算增益。soft 28→34 使"堆高单属性"的折算惩罚推迟，玩家更易把某一创作力推过 30——**实质是强化专精/单属性 build 的评分**，而非"使其可达"。

### 补丁
`attrs.json` `diminish`：`soft 28→34`、`hard 40→48`。

### 通胀安全论证（关键）
- 六维层 `taper` 在 创作力和 51 / 基本功和 39 处半计，封顶单维评分贡献。
- `winRewardScale`（min 0.45）压低以强凌弱所得，限制单局属性暴涨。
- 仿真：P2.3 单独 mean +32，**p99 3216 与 P1 组合期 3213 几乎相同**——无 runaway，通胀可控。

### 仿真（隔离）
mean 2631 → **2663 (+32)**；其余指标不变。

---

## 四、系统交互 → 第三次重拟合带宽

P2.1(+25) + P2.3(+32) 组合 mean **+58**（2631→2689，p90 2970→3020）。当前带宽下 文宗 飙到 **8%**，临界失衡。按既定方法论（分布位移即重拟合）再次反推：

组合分布百分位：p04 1925 / p12 2218 / p26 2577 / p42 2730 / p50 2775 / p58 2818 / p74 2903 / p87 2990 / p95 3083 / p99 3216。

已写入 `grades.json` 九档：

| 品级 | 童生 | 秀才 | 举人 | 进士 | 探花 | 榜眼 | 状元 | 翰林 | 文宗 |
|---|---|---|---|---|---|---|---|---|---|
| min | 0 | 1950 | 2200 | 2600 | 2750 | 2800 | 2900 | 3000 | 3100 |
| max | 1949 | 2199 | 2599 | 2749 | 2799 | 2899 | 2999 | 3099 | null |

**最终实测（全量生效 + 新带宽，sim_grade_refit 复跑）：**
童生 4.6% / 秀才 7.7% / 举人 15.4% / 进士 17.7% / 探花 8.9% / 榜眼 18.0% / 状元 14.5% / 翰林 8.5% / **文宗 4.7%**。

→ 九档全活、轻底满中尖顶，文宗稳定可达的精英档（~5%），中段（探花/榜眼/状元）密集承载主流量。

---

## 五、改动清单（纯数据/局部代码，可逐条回滚）

| 文件 | 改动 |
|---|---|
| `config/grades.json` | `jiecai.turns_max` 34→38（desc 同步）；九档带宽第三次重拟合 |
| `config/attrs.json` | `diminish` soft 28→34、hard 40→48 |
| `js/engine/rules.js` | lap2 `difficultyWeights` d3 0.40→0.30（rebalance 2→0.55） |

---

## 六、Playtest 失败信号（更新）

1. **封笔率 > 3%** → 灵感经济过紧（当前 0.8%，安全）。
2. **文宗达成率 > 8% 或 = 0** → 顶端带宽错位（当前 4.7%，健康）。
3. **榜眼 > 30%** → 中段过载（当前 18%，健康）。
4. **任一主动天赋胜率差 > 15pp** → 超模（重点盯修复后的骰子卡）。
5. **school 选择对终品级相关性 ≈ 0** → 流派系统未生效（已正相关）。
6. **lap2 长局重复题感知强** → d3 仍不足 → 触发内容扩充（P2.2 仅缓解）。

---

## 七、未决 / `[PLACEHOLDER]`

- **d3 内容扩充**：权重缓解 ≠ 根治，需内容负责人补 ≥8 道 d3 题。
- **探花带宽偏窄（50 宽 / 8.9%）**：因该分段分数密度高，属分布使然，非缺陷；若追求视觉均匀可微调，但会牺牲"按分布拟合"的原则。
- **专精 68% 仍偏高**：流派分区分度低。若要让门派稀缺，可上调 `attrMin`/`winMin`（见 P1 报告延伸建议），本次未动。
- **真实分布校准**：仿真用 stub UI（抉择恒有效、支线按 `insp≥cost+8` 自动进），上线后按实机分布微调带宽（本文件数值均为 `[PLACEHOLDER · 附验证路径]`）。
- 不承诺"会火"——只承诺循环好玩 + 每条数值有验证路径。
