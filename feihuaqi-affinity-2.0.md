# 相性系统 2.0 —— 策略性 × 变化性重设计

> 目标：让飞花棋的「相性（题材×风格）」从「单一战斗 ±5%/±10% 加值」升级为有策略纵深、且每局最优解不同的系统。
> 改动均为**纯配置 + 局部引擎/UI**，可逐条回滚；无公式结构破坏。

---

## 一、现状诊断（为什么旧系统既不策略也不变化）

旧 `affinity.json` 是 3 风格 × 6 题材的稀疏矩阵，仅 13/18 格非 0，数值 `{-0.05, 0, +0.05, +0.10}`，且只作为战斗分的一个 `pct` 修正，NPC 也自动选最优风格 → 玩家优势被抵消。

仿真实测（旋钮归零的纯旧矩阵）暴露两个硬伤：

1. **变化性塌缩**：主导风格分布 `婉约 78% / 豪放 2% / 哲理 20%`——婉约因是 3 个题材的唯一最优，几乎垄断所有战斗。三风格严重不均，谈不上「策略取舍」。
2. **决策无意义**：大量「中平 0」格让许多战斗的风格选择对结果无影响；相性只作用于战斗，与答题/奇遇/流派完全脱钩，是狭窄的单一杠杆。

---

## 二、设计：四层叠加（每层独立旋钮、可单独下调）

| 层 | 机制 | 作用 | 为何增加策略/变化 |
|---|---|---|---|
| **① 稠密非对称矩阵** | 每格三档 `契合+0.12 / 相得+0.06 / 中平0 / 相左-0.08`；每题材**唯一最优**风格、三风格**各自有 ≥1 最优题材** | 基础相性 | 每场战斗的风格抉择都是真实取舍，不再有「随便选都行」的格子 |
| **② 门派文风（identity）** | 学派 `homeManner`：用本门风格额外 `+0.04`（通儒为 `adaptive`：临题自选最优 `+0.04`） | 身份层 | 选门派=选战斗身份；「忠于本门 vs 迁就题材最优」成为取舍 |
| **③ 当朝风潮（zeitgeist）** | 开局随机抽「热点题材(+0.04 全风格)」+「得势文体(+0.03 全题材)」 | 变化层 | **每局最优解不同** → 重玩变化性核心来源 |
| **④ 气势连捷（momentum）** | 连续同风格**胜场**叠加 `+0.02/层`，封顶 5 层(+0.10)；换风格或败清零、平局保留 | 复利层 | 「专精一门 vs 每题自适应」的核心策略张力 |

**关键设计约束**：NPC 仍只用①基矩阵（引擎 `affinityValue`），②③④ 是**玩家专属优势层**——这是把「理解并驾驭相性」转化为胜势的奖励，而非免费增益。

---

## 三、具体改动清单（可逐条回滚）

**`config/affinity.json`**（重写）
- `matrix`：18 格全部赋值，非对称、每题材唯一最优、三风格各 2 最优题材。
  - 豪放最优：山水、边塞｜婉约最优：咏物、送别｜哲理最优：怀古、节令。
- 旋钮：`homeMannerBonus:0.04`、`homeAdaptiveBonus:0.04`、`zeitgeistThemeBonus:0.04`、`zeitgeistMannerBonus:0.03`、`momentumPer:0.02`、`momentumMax:5`。

**`config/schools.json`**（每学派加 `homeManner`）
- 诗仙→豪放、词宗→婉约、联圣→哲理、通儒→`adaptive`、奇士→豪放。

**`js/engine/rules.js`**（新增纯函数，保持 `affinityValue`/`affinityStars` 兼容 NPC）
- `effectiveAffinity(matrix,manner,theme,schoolHome,zeitgeist)`：①+②+③ 综合。
- `bestMannerForTheme(matrix,manners,theme)`：通儒自适应判定。
- `momentumPct(streak,manner,af)`：④。
- `affinityTierLabel(v)`：契合/相得/中平/相左 文案。

**`js/engine/game.js`**
- `start()`：播种 `zeitgeist`（每局随机）、初始化 `affStreak`。
- `createSession`：`affinityOf` 改为 `effectiveAffinity`；暴露 `schoolHome/homeResolved/zeitgeist/themeNames` 与 `momentumPre(m)`。
- `resolveBattle`：相性拆成 基矩阵 / 本门功底 / 风潮(热点+得势) / 气势连捷 四个独立 `pct` 叠加。
- `settleBattle`：维护连捷状态（胜累加、换风格/败清零、平局保留）。

**`js/ui/battle.js` + `css/ui.css`**
- 审题面板：显示「当朝风潮」「本门文风」提示。
- 选风格卡片：显示每风格**有效相性%与档位**、本门标注、选此风格的**气势预览**，高亮综合最优。

---

## 四、仿真验证（`sim_affinity.mjs`，2000 局/场景，确定性 RNG）

| 指标 | 旧(稀疏矩阵) | 新相性2.0 | 解读 |
|---|---|---|---|
| 胜率 | 74.4% | **80.5%** | 驾驭相性的合理回报（最优操作上限，真实玩家更低） |
| 均值 | 2626 | 2680 | +54，量级温和 |
| 标准差 sd | 338 | **316** | 反而收紧，非雪球 |
| 封笔率 | 0.9% | 0.7% | 健康 |
| **主导风格** | 婉约78/豪放2/哲理20 | **婉约49/豪放22/哲理28** | ✅ 三风格均衡可行 |
| 风潮组合多样性 | — | **18/18 全出现** | ✅ 每局最优解不同 |
| 平均最大连捷 | 2.4 | 2.5 | 气势触发但非主导（加分项，非主机制） |

**九档达成率（新系统，实机带宽）**：童生3% / 秀才6% / 举人21% / 进士19% / 探花10% / 榜眼17% / 状元12% / 翰林7% / 文宗4% —— 金字塔健康，无过载（榜眼<30%），文宗稳定可达。

低熟练(0.55)场景同样健康：胜率 78.1%、封笔 1.1%、主导风格仍均衡（51/22/27）。

---

## 五、可调旋钮（如需再平衡）

| 想调的效果 | 改哪个 | 方向 |
|---|---|---|
| 削弱门派身份权重 | `homeMannerBonus` | 0.04 → 0.03 |
| 削弱每局差异 | `zeitgeistThemeBonus`/`zeitgeistMannerBonus` | 下调或设其一为 0 |
| 削弱连捷滚雪球 | `momentumPer` / `momentumMax` | 0.02→0.015 / 5→4 |
| 整体难度 | 矩阵档位 `+0.12/+0.06/-0.08` | 整体 ±0.01 微调 |

> 所有数值均带 `[PLACEHOLDER · 附验证路径 sim_affinity.mjs]`，实机 playtest 后按真实分布微调即可。

---

## 六、Playtest 验收信号（沿用全案）

- 封笔率 >3% → 相性加成过弱或灵感经济失配。
- 某一风格主导率 >70% → 矩阵仍不对称不足，需再调。
- 榜眼 >30% 或 文宗 =0 / >8% → 带宽失配，需重拟合。
- 不同风潮局的最优风格明显不同（主观验收变化性）。

## 七、尚未覆盖（超出本次范围，建议后续）

- **答题相性 tie-in**：让「风格」也轻度影响创作抉择（如本门风格抉择额外 +1 属性/灵感），使相性从纯战斗扩到全局身份。需给玩家一个持久「文风」设定入口（UI 工作量较大）。
- **相性天赋扩充**：现有 `copy_affinity`（夺胎换骨）可保留；可新增「风潮共振」（本局风潮加成翻倍）、「一气呵成」（连捷封顶+2）等相性向文心，深化构筑。
