# 文心升级功能 × 编辑器系统 兼容性检查

> 检查对象：`feihua-editors/`（七合一内容编辑器，文心模块 `assets/js/talent.js`）
> 对照物：`feihuaqi-playable/config/talent-upgrade.json` + 引擎 `game.js` 的 `leveledTalent`/`upgradeTalent`
> 检查时间：2026-08-15

## 一、结论

**部分兑现，存在结构性缺口。**

- ✅ 编辑器能编辑文心的「身份/叙事字段」并导出 `talents.json`；
- ✅ 升级系统用到的**全部 effect 类型**都在编辑器下拉中；
- ❌ 升级系统的核心数据结构（品质档 `quality` / `maxLevel` / `upCost` 成本曲线 / 逐级 `levels[]` effect）在编辑器中**完全不可编辑、也不可导出**；
- ⚠️ 升级型文心在编辑器里改 `effect` 会被游戏侧**静默覆盖**（不生效）。

目前 `talent-upgrade.json`（41+ 枚）只能由 `gen_talent_upgrade.py` + Excel v2 设计稿生成，编辑器不参与其生产。

## 二、逐项核对

### ✅ 已可在编辑器兑现

1. **effect 类型全覆盖**
   升级用到的 `attr_flat`、`start_insp`、`insp_max`、`dice_mult`、`copy_affinity`（以及 `palace_pct`、`on_win_bonus`、`dice_plus` 等）都已收录在 `talent.js` 的 `TALENT_TYPES`，且 `effectDyn()` / `normalizeEffect()` / `talentEffectText()` 均有对应渲染与校验。
2. **文心身份字段**
   `id` / `name` / `kind` / `school` / `text` / `source` / `acquire` 编辑器可编辑，`exportData()` 导出 `talents.json`，放回游戏 `config/` 即生效。这些字段正是引擎 `leveledTalent` 克隆的基底（`clone = {...talent}`，game.js:333）。

### ❌ 编辑器无法兑现（缺口）

3. **品质档 `quality`**
   升级系统按 `common / rare / epic / legend` 决定 `maxLevel`（3/4/5/6）与成本基准；编辑器文心模型**无 `quality` 字段**（`talent.js` 全文件无 `quality`/`rarity`，`rarity` 仅奇遇/NPC 用，且奇遇只有 3 档、不含 `epic`）。
4. **`maxLevel` / `upCost` 成本曲线**
   编辑器无对应输入项，也无导出。
5. **逐级 effect 数组 `levels[]`**
   升级系统的「每级效果」是 `talent-upgrade.json` 的 `levels:[{effect, cost?}, ...]`（由 Lv1→满级端点插值生成）；编辑器每枚文心只存**单个** `effect`，无法表达多级曲线。
6. **导出目标单一**
   `talent.js` 的 `exportData()` 只下载 `talents.json`，从不产出 `talent-upgrade.json`。
   全工程 grep 确认：`feihua-editors/` 内无 `talent-upgrade` / `maxLevel` / `upCost` / `upgradeTalent` 等任何升级相关标识（仅奇遇/NPC 有 `rarity`，羁绊/NPC 有无关 `levels`）。

### ⚠️ 隐性陷阱（需提防）

7. **effect 编辑在升级型文心上「静默失效」**
   游戏 `leveledTalent`（game.js:334-337）：当 `talentUpgradeById` 命中该 id，用 `up.levels[level-1].effect` **覆盖** `talents.json` 的 `effect`。
   → 只要该文心在 `talent-upgrade.json` 有条目，在编辑器里改它的 `effect` 数值**不会改变**游戏内实际效果（仍取升级配置）。作者容易误以为改了生效。
8. **双源漂移**
   `talents.json`（编辑器产）与 `talent-upgrade.json`（py + Excel 产）是两份独立文件，当前无自动同步。改文心效果/名称需两处各改。

## 三、生产链路现状

- `talent-upgrade.json` 由 `gen_talent_upgrade.py` 从 Excel v2 设计稿生成 → 落 `feihuaqi-playable/config/`。
- 编辑器七合一仅对 `talents.json` 负责；`talent-upgrade.json` 不在编辑器职责内。

## 四、若要让编辑器完整兑现（最小改动清单）

- A. `talent.js` 数据结构增加 `quality` 字段 + `upgrade` 子对象（`{maxLevel, upCost[], levels[]}`），或加「可升级」开关。
- B. 编辑器 UI 增加升级面板：品质下拉、`maxLevel`、`upCost` 多输入、逐级 effect 编辑器（复用现有 `effectDyn`）。
- C. `exportData()` 同时产出 `talents.json` 与 `talent-upgrade.json`（新增 `exportUpgrade()`）。
- D. 校验对齐：`quality→maxLevel` 映射、`upCost` 长度 = `maxLevel`、Lv1 effect 与 `talents.json` 一致（消除陷阱 7）。
- E. 导入：能读取 `talent-upgrade.json` 回填升级面板。

> 这是一块中等体量改动（UI + 导出 + 校验），建议确认范围后再做。
