# 《飞花棋》“终局成卷”系统细化方案

> 版本：1.0
>
> 日期：2026-09-01
>
> 定位：“一路成诗·诗意回响”方案的终局兑现模块
>
> 用途：供策划、文案、程序、UI 与测试共同评审和实施
>
> 状态：设计方案，不包含代码与配置改动

## 1. 设计结论

“终局成卷”不应是系统在结算时临时生成一首看似完整、实则与本局关系不大的诗，也不应要求玩家在终局再次答题或自由输入。

推荐采用：

```text
沿途素材留痕 → 离开每圈时择一句定稿 → 终局按本局结局合卷 → 结算页揭卷与保存
```

具体规则如下：

1. 全局只形成一份“本局行卷”，不新增收藏背包、碎片库存或成卷货币。
2. 玩家离开外圈、中圈、内圈时，各从两句候选中保留一句；候选默认选中，因此不增加强制点击。
3. 终局不再要求操作，只把已经定下的 1—3 句、终局结句和一段卷后小记组合成卷。
4. 全部正文采用策划预写的完整句模板，不在运行时自由拼接半句，不依赖联网生成模型。
5. 行卷只提供表达、回忆和分享价值，不改变总评、段位、排行榜或跨局属性。
6. 现有结算页的“行卷留痕”与“卷末余音”并入成卷卡，避免内容重复和页面膨胀。

系统最终要让玩家感到：

> 这不是游戏替我写的一首诗，而是游戏把我这一路真正留下的几笔收在了一起。

## 2. 设计目标与边界

### 2.1 体验目标

终局成卷承担四项体验职责：

| 目标 | 玩家感受 | 设计手段 |
|---|---|---|
| 回忆 | “我记得这一路发生过什么” | 引用本局诗景、抉择、关键论战和支线结果 |
| 表达 | “这几句确实更像我的写法” | 每圈提供两句无优劣候选，允许玩家择句 |
| 收束 | “我的旅程在这里有了结尾” | 按金榜、桃源、封笔等不同结局配置结句 |
| 分享 | “这一局有一张值得保存的纪念” | 在现有成绩图中加入简洁成卷版式 |

### 2.2 明确不做

首版不包含：

- 玩家自由输入或逐字作诗；
- 平仄、押韵、格律评分；
- 诗句碎片收集与库存管理；
- 成卷兑换属性、文心或名篇；
- AI 对玩家作品进行文学高低评价；
- 为了凑成固定篇幅而虚构玩家没有经历的事件；
- 在终局增加新的必选弹窗。

### 2.3 MDA 对应

- **机制**：每圈择一句，系统记录来源，终局按规则合卷。
- **动态**：玩家开始留意沿途诗景和自己的表达倾向，并期待它们在终局回响。
- **审美体验**：从“查看一组统计数据”转化为“读完一段属于本局的行旅记忆”。

成卷的核心奖励是自主表达、旅程纪念和好奇心满足，而不是额外数值。

## 3. 玩家流程

### 3.1 一局中的完整流程

```text
外圈经历
  ↓
离开外圈：两句候选中默认选一句，可主动改选
  ↓
中圈经历
  ↓
离开中圈：同上
  ↓
内圈经历
  ↓
进入殿试：同上
  ↓
普通终局 / 金榜终局 / 桃源终局
  ↓
结算页自动揭示“本局行卷”
```

### 3.2 为什么按圈而不是按每次晋阶试定稿

当前三圈地图天然对应三段旅程。按外圈、中圈、内圈各保留一句有三个好处：

1. 玩家容易理解“走完一圈，留下一笔”。
2. 最终正文天然最多三句，篇幅稳定。
3. 不必在外圈内部每次身份变化时反复打断玩家。

第一处外圈内部晋阶点只展示一条“行卷初评”，不要求定稿；真正离开外圈时再完成第一句。

### 3.3 零额外强制点击

在现有阶段过场中展示两句候选：

```text
此程将尽，哪一句留在行卷里？

● 柳色留人，孤帆已远。
○ 长亭无语，雁带秋声。

[继续入中圈]
```

- 系统预先根据本圈经历默认选中更匹配的一句。
- 玩家可以改选，也可以直接点击原有的“继续”按钮。
- 不设置倒计时；若界面意外关闭或流程中断，保存默认项。
- 两句没有不同数值奖励，不出现“推荐”“稀有”或星级。

## 4. 成卷成品结构

### 4.1 标准结构

```text
《柳色照长风》
无名氏 · 本局行卷

柳色留人，孤帆已远。
孤烽照夜，落笔仍有长风。
金殿回首，旧砚上尚留春雨。

卷后小记
此卷多见送别与边塞，笔意清雅而渐趋沉郁。
你在榜下没有收笔，那一夜后来成了卷中最深的一点墨。

［金榜题名］
```

### 4.2 信息组成

| 区域 | 内容 | 长度约束 |
|---|---|---|
| 卷名 | 由主要意象、旅程倾向或结局选择一个预写标题 | 4—7 字 |
| 署名 | 玩家名号；为空时显示“无名氏”或“你” | 最多 12 字 |
| 行卷正文 | 已定稿的圈层句，按外→中→内排列 | 1—3 句，每句 8—18 字 |
| 终局结句 | 对当前结局进行收束，不冒充玩家圈层定稿 | 1 句，8—20 字 |
| 卷后小记 | 描述常见题材、文风和一件代表经历 | 40—90 字 |
| 结局印章 | 金榜题名、万卷归心、终问未竟等 | 4—6 字 |

若正文第三句本身已经承担结局收束，可以省略独立终局结句，避免重复。

### 4.3 行卷不是格律诗

界面统一使用“行卷”“行卷摘句”或“本局行卷”，不使用“绝句”“律诗”“玩家诗作”等称呼。

原因是句子需要优先忠实记录玩家经历，未必满足统一字数、押韵和平仄。明确体裁边界可以减少玩家把它当作格律考试，也给文案留下自然表达空间。

## 5. 素材来源与优先级

### 5.1 可复用的现有素材

当前版本已经记录了多类可直接使用的本局事实：

| 数据 | 可表达的内容 | 当前可用性 |
|---|---|---|
| `choiceHistory` | 创作抉择、修习方向、墨痕倾向、选择结果 | 已存在，可直接使用 |
| `narrativeState.eventChoices` | 奇遇中真正选择过的行动及其回声 | 已存在，可直接使用 |
| `sideQuest` | 名胜支线路线、终选和结局 | 已存在，可直接使用 |
| `battle` | 胜负、连胜、逆袭、三文体胜场 | 已存在，但主要为汇总数据 |
| `quiz` | 考课次数与正确数 | 已存在，只作次要小记，不主导正文 |
| `phaseGateSeen`、`routeIndex` | 玩家实际走到哪一圈、完成哪些阶段 | 已存在，可用于控制篇幅 |
| `endReason`、`secretFinal` | 金榜、封笔、桃源胜负等结局 | 已存在，可决定结句和印章 |

### 5.2 建议新增的轻量记录

为了让终局能够引用具体题材、文风和关键论战，建议只新增一个受控状态 `poetryState`，不保存大段动态文本：

```json
{
  "version": 1,
  "chapterDrafts": {
    "outer": {
      "candidateIds": ["OUT_SONGBIE_01", "OUT_SONGBIE_02"],
      "selectedId": "OUT_SONGBIE_01",
      "lockedAtTurn": 18
    },
    "middle": {
      "candidateIds": ["MID_BIANSAI_01", "MID_INK_RANBI_01"],
      "selectedId": "MID_BIANSAI_01",
      "lockedAtTurn": 34
    },
    "inner": {
      "candidateIds": ["IN_PALACE_01", "IN_QINGYA_01"],
      "selectedId": "IN_QINGYA_01",
      "lockedAtTurn": 48
    }
  },
  "moments": [
    {
      "type": "battle",
      "chapter": "middle",
      "turn": 29,
      "refId": "npc_id",
      "theme": "biansai",
      "manner": "qingya",
      "result": "upset"
    }
  ]
}
```

约束：

- `moments` 最多保留 12 条，只记关键事实，不复制整段日志。
- 仅记录晋阶试、殿试、隐藏终圈、支线高潮、逆袭和重要失利；普通战斗不逐场记录。
- `chapterDrafts` 只保存模板 ID，不直接保存任意 HTML。
- 旧存档缺少 `poetryState` 时补空结构，不追溯补发奖励。

### 5.3 素材优先级

候选句取材优先级建议为：

1. 玩家在“诗意回响”中主动选择并成功化用的题眼；
2. 本圈代表性的创作抉择与墨痕；
3. 支线终选、晋阶试、殿试或逆袭；
4. 本圈出现频率最高的题材与文风；
5. 圈层通用行旅句；
6. 考课表现。

知识考课不应成为行卷正文的主要来源，否则新系统仍会退化成“答题成绩换一首诗”。考课只适合出现在卷后小记，例如“你曾在书院辨清旧句，到了江上却选择写自己的月色”。

## 6. 候选句生成规则

### 6.1 使用完整句模板

每条候选都是经过审核的完整句，不允许把“意象前半句”和“行为后半句”任意交叉拼接。

推荐配置在现有 `narrative.json` 下新增 `endScroll` 区块，避免再增加一套配置入口：

```json
{
  "endScroll": {
    "version": 1,
    "chapterLines": [
      {
        "id": "OUT_SONGBIE_01",
        "chapter": "outer",
        "requires": { "themes": ["songbie"] },
        "prefers": { "manners": ["wanyue", "qingya"] },
        "motif": "柳",
        "tone": "惜别",
        "text": "柳色留人，孤帆已远。",
        "weight": 10
      }
    ],
    "titles": [],
    "endingLines": [],
    "notes": []
  }
}
```

### 6.2 候选评分

每圈结算时，对适用模板计算一次匹配分：

```text
候选分 = 明确选择来源 + 题材匹配 + 文风匹配 + 墨痕匹配
       + 关键事件匹配 + 新颖度 + 基础权重
```

推荐参考值：

| 条件 | 加分 |
|---|---:|
| 来自玩家主动选过并成功化用的题眼 | +100 |
| 对应本圈支线终选或关键创作抉择 | +70 |
| 对应晋阶试、逆袭或重要失利 | +55 |
| 题材是本圈最常见题材 | +30 |
| 文风是本圈最常用文风 | +20 |
| 对应本圈主导墨痕 | +18 |
| 与前一圈意象重复 | -35 |
| 与前一圈句式结构高度相似 | -20 |

最终取分数最高且互不重复的两句作为候选。若同分，使用“本局稳定哈希”决定顺序，保证同一存档反复打开不会换句。

### 6.3 两个候选必须真正不同

同一组候选至少在以下两项上有差异：

- 观察对象不同；
- 情绪方向不同；
- 表达姿态不同；
- 句式节奏不同；
- 对应的真实经历不同。

禁止只替换一个近义词，例如：

```text
不合格：柳色留人 / 柳色送人
```

推荐：

```text
柳色留人，孤帆已远。
长亭无语，雁带秋声。
```

### 6.4 默认选中规则

默认选择匹配分更高的一句。默认不等于推荐或更优，界面不显示分数。

若两句同分：

1. 优先选择没有与前文重复意象的句子；
2. 再按本局稳定哈希选择；
3. 不使用每次打开都会变化的随机数。

## 7. 三段正文的叙事职责

三句不是并列堆景，而应形成轻微的旅程弧线：

| 圈层 | 句子职责 | 适合内容 |
|---|---|---|
| 外圈 | 起：从哪里出发、先看见什么 | 初见、求学、乡野、渡口、微小意象 |
| 中圈 | 承：怎样选择、怎样落笔 | 文风、取舍、故人、逆袭、支线与墨痕 |
| 内圈 | 转/合：带着什么进入金殿 | 考场、名望、旧愿、担当、放下与坚持 |

文案撰写时应检查三句是否都在“纯写景”。若全部只是月、柳、雁、雪，玩家经历会被景物淹没；至少一条应包含行动或取舍。

## 8. 卷名、结句与结局适配

### 8.1 卷名规则

卷名从预写标题池中整条选择，不直接用任意两个词拼接。

优先级：

1. 玩家反复出现的主要意象；
2. 最鲜明的墨痕倾向；
3. 支线或隐藏终圈主题；
4. 结局保底标题。

示例标题：

| 来源 | 标题示例 |
|---|---|
| 柳、长亭、送别 | 柳色照长风、长亭一卷 |
| 月、江、清雅 | 江月入砚、月在行舟 |
| 边塞、豪放 | 孤烽照笔、长风出塞 |
| 求真、出新 | 不借旧月、别开一卷 |
| 桃源终圈 | 万卷归心、桃花照卷 |
| 无明显素材 | 此行有墨、一路成篇 |

### 8.2 不同结局的语气

| `endReason` | 结句方向 | 印章 | 文案原则 |
|---|---|---|---|
| `jinbang` | 功成但保留一路来处 | 金榜题名 | 不只写荣耀，也回望一处旧景 |
| `taoyuan` | 超越功名、万卷归心 | 万卷归心 | 与隐藏终圈现有主题一致 |
| `secret_loss` | 金榜已定，终问未尽 | 桃径留问 | 不抹去已经取得的成就 |
| `fengbi` | 墨尽而卷未毁 | 此卷未竟 | 不羞辱、不写“才尽无用” |
| `turnlimit` | 岁月催行、尚可续笔 | 岁月留痕 | 强调未完而非失败 |
| `palace` | 已交卷、静待结果 | 金殿待榜 | 语气克制，不提前宣告胜负 |

### 8.3 进度不足时不虚构三句

- 尚未离开外圈便封笔：正文只展示 1 句。
- 已进入中圈但未到内圈：正文展示 2 句。
- 完成三圈或进入殿试：正文展示 3 句。

短卷不是惩罚，而是对本局实际进度的诚实记录。界面仍保持完整卷轴构图，通过留白表现“未竟”。

## 9. 卷后小记

### 9.1 组成方式

卷后小记由两部分组成：

```text
整体描述 1 句 + 代表经历回声 1 句
```

例如：

> 此卷多见送别与边塞，笔意由清雅渐趋沉郁。你在榜下没有收笔，那一夜后来成了卷中最深的一点墨。

整体描述只陈述事实：常见题材、常用文风、墨痕倾向变化。不得写成文学能力裁判，例如“境界低”“用典拙劣”“缺乏真情”。

### 9.2 代表经历选择

优先顺序：

1. 隐藏终圈结果；
2. 支线最终选择；
3. 殿试或晋阶试关键战；
4. 强度最高且最近的墨痕；
5. 有明确回声的奇遇选择；
6. 通用进度小记。

只引用实际发生过的事实。人物、地点和选择文本必须能追溯到记录 ID，不根据聚合统计臆造具体事件。

## 10. UI 与结算节奏

### 10.1 结算页位置

建议结算顺序调整为：

```text
品第与总评
  ↓
本局行卷（情感收束）
  ↓
六维与战绩（理性复盘）
  ↓
跨局所得（下一局动力）
  ↓
操作按钮
```

行卷卡应出现在品第之后、详细评分之前。玩家先看到“这一局留下了什么”，再查看“这一局算了多少分”。

### 10.2 合并现有内容

当前结算页已有：

- 行卷留痕；
- 卷末余音；
- 支线终局回声。

终局成卷上线后，以上内容应进入：

- 正文候选来源；或
- 卷后小记来源。

不再分别渲染三个独立长文本区块。成卷卡在移动端建议控制在首屏 280—360 px 内，卷后小记最多两段。

### 10.3 动效与声音

- 品第出现后，卷轴以 400—700 ms 展开。
- 三句按圈层每隔约 180 ms 显现，最后落结局印章。
- 使用现有纸张、落笔、印章音色，不新增一段必须等待播放完的长动画。
- 用户开启“减少动态效果”时，直接显示完整内容。

### 10.4 保存与分享

首版复用现有“生成成绩图”能力，将成卷纳入同一张长图；不新增常驻按钮。

分享图建议突出：

- 卷名；
- 1—3 句正文；
- 玩家名号；
- 品第与总评；
- 结局印章；
- 游戏名称。

六维详细拆分、跨局成长和长篇小记不进入分享图，保证可读性。

## 11. 完整示例

### 11.1 金榜题名·三句完整卷

本局事实：

- 外圈多遇送别，选择“看孤帆没入暮云”；
- 中圈边塞题逆袭，使用清雅而非豪放；
- 内圈创作抉择多次偏向“求真、出新”；
- 殿试取胜，结局为 `jinbang`。

成卷：

```text
《长风入砚》
沈砚 · 本局行卷

孤帆没入暮云，江声仍送远人。
未借鼓角壮行，孤烽也照清笔。
金殿收卷时，旧月仍在字外。

卷后小记
此卷多见送别与边塞，常以清雅写开阔之景。
你几次绕开现成名句，到了金殿，仍愿意先问自己为何落笔。

［金榜题名］
```

### 11.2 中途封笔·两句未竟卷

本局事实：

- 玩家进入中圈后灵感耗尽；
- 曾在雨中选择檐下看雨；
- 创作抉择偏向惜身，不曾进入内圈。

成卷：

```text
《檐雨未收》
无名氏 · 本局行卷

檐水将断，远山才露一痕青。
你合起旧卷，仍记得明日从哪一句读起。

卷后小记
此卷多留雨声与灯影，落笔从容，也懂得暂歇。
路没有在此消失，只是这一页尚未写满。

［此卷未竟］
```

### 11.3 桃源胜出·隐藏终卷

```text
《万卷归心》
照川 · 本局行卷

柳色过三重科场，仍带旧渡春风。
金殿之外，你把一纸功名折进行囊。
桃花落在无字处，脚下从此不劳棋盘标路。

卷后小记
你曾借古人之句，也曾放下现成答案；最后带走的不是满卷声名，而是一枝仍有来路的桃花。

［万卷归心］
```

## 12. 内容生产规范

### 12.1 句子规范

1. 使用第二人称的经历时，只放在卷后小记；正文尽量使用可独立成立的摘句。
2. 一句只承载一个主要画面或动作。
3. 每句最多一个典故来源，避免堆砌。
4. 不强求对仗，不滥用“兮”“矣”“也”等仿古语气。
5. 不直接评价玩家人格，不把任何墨痕倾向写成道德高下。
6. 引用古诗原句时必须在配置中明确作者、作品和准确文本；首版建议以原创回声为主。
7. 人名、NPC 名和玩家名不得直接拼进正文模板，只能进入经过转义的小记或署名。

### 12.2 内容兼容标签

每条模板建议至少标注：

```text
chapter / themes / manners / inkTags / momentTypes / motif / tone / ending / weight
```

另可选填：

```text
excludeWith / requiresAny / requiresAll / relatedNpcIds / relatedEventIds
```

`excludeWith` 用于阻止三句连续重复“月”“柳”“风”等高频意象。

### 12.3 首版内容量

建议最小内容包：

| 内容 | 数量 |
|---|---:|
| 六题材×三圈基础句 | 18 |
| 八种墨痕倾向代表句 | 8 |
| 关键战/支线通用句 | 8 |
| 六类结局×两种结句 | 12 |
| 卷名 | 12—18 |
| 卷后小记模板 | 12 |

约 70 条短文本即可支撑首版。先保证组合自然，再扩充稀有文本。

## 13. 程序职责建议

### 13.1 引擎层

新增一个纯逻辑组合器，例如：

```text
buildEndScroll(state, narrative.endScroll) → endScrollSummary
```

输出结构建议：

```json
{
  "version": 1,
  "title": "长风入砚",
  "byline": "沈砚",
  "lines": [
    { "chapter": "outer", "templateId": "OUT_SONGBIE_02", "text": "……" },
    { "chapter": "middle", "templateId": "MID_BIANSAI_03", "text": "……" },
    { "chapter": "inner", "templateId": "IN_QIUZHEN_01", "text": "……" }
  ],
  "endingLine": "……",
  "note": "……",
  "seal": "金榜题名",
  "sourceRefs": ["Qxxxx", "Exxxx", "npc_id"]
}
```

要求组合器：

- 不访问 DOM；
- 相同状态与相同模板配置得到相同结果；
- 配置缺失时返回安全保底卷；
- 不改变分数和状态中的战斗数值；
- 只输出纯文本与来源 ID。

### 13.2 UI 层

UI 只负责：

- 转义并渲染 `endScrollSummary`；
- 展开动效和印章反馈；
- 将同一结构传给成绩图；
- 在旧版总结缺少成卷时隐藏该区域。

禁止 UI 自行重新抽模板，否则结算页和分享图可能出现不同文本。

### 13.3 存档层

- 将 `poetryState` 加入受控状态白名单。
- 反序列化时只接受合法模板 ID、合法圈层和有限长度数组。
- `moments` 截断为最近或权重最高的 12 条。
- 旧存档自动补空结构，不提高存档版本也可以兼容时，应优先走字段缺省；若修改迁移契约则同步提升版本。

### 13.4 现有工程落点

| 文件/模块 | 可复用能力 | 成卷实施职责 |
|---|---|---|
| `js/engine/game.js` | `choiceInkSummary()`、`choiceInkEpilogue()`、阶段门与 `endGame()` | 记录关键时刻、锁定圈层句、构建 `endScrollSummary` |
| `js/ui/modals.js` | `showStageChange()`、殿试与结局弹窗 | 在原阶段过场中展示两句候选，不新开独立弹窗 |
| `js/ui/app.js` | `showResult()` | 将成卷卡放在品第之后，并移除重复的旧回声区块 |
| `js/ui/album.js` | 现有成绩图生成 | 复用同一 `endScrollSummary` 输出分享图 |
| `js/engine/save.js` | 状态白名单、旧档归一化和截断 | 安全保存、清洗 `poetryState` |
| `config/narrative.json` | 序章、阶段、殿试和隐藏结局文案 | 承载 `endScroll` 模板池和保底文案 |
| 内容编辑器叙事模块 | 云端覆盖与内容发布 | P0 可先支持原始 JSON；内容稳定后再增加结构化表单 |

所有逻辑仍应以可玩版实际目录为准；根目录聚合内容包同步与缓存版本更新属于实施阶段的正常发布步骤。

## 14. 异常与降级

| 情况 | 降级行为 |
|---|---|
| 本圈没有诗景、抉择或关键战 | 使用对应圈层的通用行旅句 |
| 只产生一个合法候选 | 该句默认选中，第二项使用通用候选 |
| 模板 ID 在更新后失效 | 按已记录标签重新选句；无标签则用通用句 |
| 玩家中途封笔 | 只显示实际完成圈层数量的正文 |
| 玩家名为空 | 署名显示“无名氏”或不显示署名，不把“你”当姓名 |
| 玩家名含特殊字符 | 按普通文本转义，禁止进入 HTML |
| 所有终局模板缺失 | 显示“此行有墨，来路可寻。”与对应结局印章 |
| 成绩图生成失败 | 不影响结算页和重新开始游戏 |

## 15. 验收标准

### 15.1 功能验收

- 离开外圈、中圈、内圈时各生成两句合法且不同的候选。
- 不改选时可以沿用现有“继续”按钮完成流程，不增加强制点击。
- 玩家改选后，终局正文必须使用所选模板 ID。
- 同一存档重复读取、重复打开结算，成卷内容保持一致。
- 六类 `endReason` 都能得到对应结句和印章。
- 中途结束时不伪造尚未经历圈层的正文。
- 成卷生成不改变总分、属性、灵感、解锁和排行榜数据。
- 旧存档和缺少 `endScroll` 配置的自定义工程可以安全降级。

### 15.2 内容验收

- 三句之间无明显意象、句式或结尾词重复。
- 至少一句对应玩家的真实行动或取舍，而非全部纯写景。
- 卷后小记引用的事件、NPC、题材和结局都能追溯到本局记录。
- 不出现系统未记录的虚构胜利、人物关系或支线结果。
- 不使用道德高下或文学能力羞辱性评价。
- 所有引用古诗均经过出处与文字校对。

### 15.3 UI 验收

- 移动端成卷主体在约 280—360 px 高度内可读完。
- 成卷位于品第之后、详细六维评分之前。
- 现有“行卷留痕”“卷末余音”和支线回声不再重复独立展示。
- 减少动态效果模式下无需等待动画。
- 分享图在常见手机宽度下不截断卷名、正文、品第和印章。

### 15.4 试玩指标

首轮试玩建议观察：

- 阶段候选主动改选率：目标 25%—60%；过低说明两句差异不明显或玩家没注意到。
- 终局成卷展开后的停留时长：目标中位数 8—20 秒。
- 成绩图生成率相对旧版是否提升。
- 玩家能否在复盘访谈中说出至少一句自己留下的摘句。
- 玩家是否误以为两句有数值优劣；若比例超过 15%，需强化“只定表达、不改得失”的提示。

## 16. 分期实施

### P0：最小闭环

1. 在 `narrative.json` 增加 `endScroll` 模板区。
2. 增加 `poetryState.chapterDrafts` 和有限的 `moments`。
3. 在离开三圈时生成两句候选并保存默认/玩家选择。
4. 在 `endGame()` 中构建唯一的 `endScrollSummary`。
5. 结算页合并“行卷留痕”“卷末余音”和支线终局回声。
6. 成绩图加入卷名、正文与结局印章。
7. 增加组合器、存档、六类结局和移动端展示测试。

### P1：内容扩展

- 为重要 NPC、名胜支线和传世名篇增加专属稀有句。
- 让成功化用的诗意题眼拥有更高候选优先级。
- 增加跨局“本次未见的新句”权重，但仍不建立诗句库存。
- 允许从结算页查看三圈当时的另一个候选，仅供回看，不改最终结果。

### P2：分享与收藏优化

- 成绩图提供“行卷版 / 数据版”两种布局。
- 图鉴只记录完成过的卷名、结局印章与日期摘要，不收藏每条句子。
- 为桃源终圈等高价值结局增加专属纸张、印色或边框外观。

## 17. 最终判断

“终局成卷”最重要的不是生成文本的数量，而是建立清楚的因果链：

```text
我经历了什么
→ 系统给出与经历相关的表达
→ 我亲自留下其中一句
→ 终局确实把它收进我的行卷
```

因此首版应坚持三个原则：

> 句子来自真实经历；
>
> 选择只决定表达，不决定强弱；
>
> 终局负责收束，不再增加负担。

在这一边界内，成卷能够显著增强诗词参与感、终局记忆和分享意愿，同时不会把《飞花棋》变成作诗编辑器或另一套需要管理的养成系统。
