# 桃花岛·飞花棋 —— P1-1（流派倒置）+ P1-3（答对回灵感）落地与验证

> 设计师视角：先结论，再论据。每项改动配 rationale，仿真背书（脚本 `sim_p1_fixes.mjs`，每场景 2000 局，自动最优战斗、答题率 75%）。
> 依赖前置：P0（败中有得/null dice_mult）已生效，P1-2（品级带宽）已按修复后分布重拟合。

---

## 结论先行

- **P1-1**：专精档（诗仙/词宗/联圣）score **150 → 210**，高于三绝 180。修正"专精 < 均衡"倒挂，让开局选门派成为真实取舍。
- **P1-3**：答对 / 有效抉择 **+1 灵感**（config `quizCorrectInsp:1`，引擎默认 `?? 0` 即关，安全可拨）。把核心技能与燃料可持续性耦合。
- **关键联动**：P1-3 触发**二阶效应**——更多灵感余量 → 玩家能进更多支线 → 奇遇分整体抬高，使总分分布上移 ~100 分。叠加 P1-1（+38），**分布整体右移 ~142 分**，原 P1-2 带宽再次失配（文宗 飙到 14%）。**已按组合后分布二次重拟合带宽**，品级金字塔恢复健康（文宗 4.9%）。

---

## 一、P1-1：流派分"专精 < 均衡"倒挂

### 事实（代码层，非猜）
`rules.js` 的 `adaptGradesConfig` 对 `liupai` 维度只读 **`shixian.score`** 映射到 `specialist.bonus`；`cizong`/`liansheng` 的 `score` 不被引擎读取（镜像字段）。`sanjue.bonus` 读 `sanjue.score`。结算时 `best = max(specialist, sanjue)`。

- 旧：specialist 150 < sanjue 180。
- 玩家若"专精但三项均 ≥14"（如 shi30/ci14/lian14），**两者都达标，取高的 180**；只有把另两项压到 <14 的"硬专精"才拿 150。
- 结果：school 引导专精，评分却奖励均衡——方向打架，门派身份在评分里是负期望。

### 补丁
`grades.json` 维度5 `liupai.tiers` 三个专精档 `score` 150 → **210**（功能上仅 shixian 生效，三者同改仅为配置自洽）。sanjue 维持 180。

### rationale
专精条件（单创作力 > 另两项和 且 ≥30 且 该文体胜 ≥3）明显严于三绝（三项均 ≥14）。更高门槛应得更高回报，210 相对 180 留 +30 溢价，使"一门深入"成为明确的高风险高回报身份路径。

### 仿真影响（隔离，P1-1 only）
| 指标 | baseline | P1-1 only |
|---|---|---|
| mean | 2500 | 2538 (+38) |
| 文宗达成 | 3% | 6% |
| 专精/三绝达成率 | 68% / 20% | 不变 |

> `[PLACEHOLDER · 待 playtest]`：专精达成率实测 **68%**（多数玩家本就走门派），说明专精已是主流路径，流派分的"区分度"偏低。若要让门派更稀缺尊贵，后续可上调 `attrMin 30→32` 或 `winMin 3→4`（属 P1-1 的延伸，非本次范围）。

---

## 二、P1-3：灵感经济——"技能不回燃料"

### Sources / Sinks 现状（实测 config）
| 方向 | 项目 | 值 | 触发 |
|---|---|---|---|
| Source | 平韵格 | +1 | 随机落点（~30% 格） |
| Source | 奇遇效果 | 偶发 | 事件 effect.inspiration |
| **Source(新)** | **答对 / 有效抉择** | **+1** | **P1-3** |
| Sink | 应战 | −2 | 每场必扣（进入即扣） |
| Sink | 败北 | −3 (late) | 战败追加 |
| Sink | 支线 | −4 | 踏入支线 |
| Sink | 答错/超时 | −1 | 答错 |
| Sink | 文心 | 扣费 | 激活文心 |

胜利**不给灵感**（只给属性）→ 战斗是净消耗，属刻意压力。P1-3 **只补"答对回馈"缺口，不碰任何大盘**（initial/max/branchCost/battleCost 全不动）。

### 补丁
- `config/inspiration.json` 增 `"quizCorrectInsp": 1`。
- `js/engine/game.js` `doQuiz`：知识答对支路、抉择有效支路各加
  `this.addInspiration(this.cfg.inspiration.quizCorrectInsp ?? 0, '答对'/'抉择');`
- 默认 `?? 0`：配置缺省即关闭，特性由显式配置开启，**安全可拨、零风险**。

### rationale
`quizWrong = −1`；对称 `+1` 使"答题表现"对灵感**净中性**——skill 自我维持，正是核心循环该有的"skill→sustain"闭环。战斗/支线仍是真实消耗，策略张力保留。

### 仿真影响（隔离，P1-3 only）——含意外二阶效应
| 指标 | baseline | P1-3 only |
|---|---|---|
| mean | 2500 | **2604 (+104)** |
| 封笔率 | 1.1% | **0.2%** ✅ |
| 平均余灵 | 6 | 10 ✅ |
| 文宗达成 | 3% | 10% ⚠️ |

**二阶效应**：余灵从 6→10，越过支线进入阈值（stub：`insp ≥ cost+8`）的玩家变多 → 支线进入率↑ → 奇遇分↑ → 总分整体 +100。这是"技巧→探索"的正向外溢，**方向上好**，但代价是必须把品级带宽一起抬高（见第三节）。

---

## 三、系统交互 → 必须二次重拟合带宽

P1-1 + P1-3 组合分布：`mean 2642 / sd 335 / p10 2132 / p50 2724 / p90 2988 / p95 3063 / p99 3213`。原 P1-2 带宽（文宗 2950+）使文宗暴涨到 **14%**，顶端再次虚设。

按组合后分布百分位反推连续带宽（沿用 P1-2 方法），已写入 `grades.json`：

| 品级 | 童生 | 秀才 | 举人 | 进士 | 探花 | 榜眼 | 状元 | 翰林 | 文宗 |
|---|---|---|---|---|---|---|---|---|---|
| min | 0 | 1900 | 2200 | 2500 | 2650 | 2750 | 2850 | 2950 | 3050 |
| max | 1899 | 2199 | 2499 | 2649 | 2749 | 2849 | 2949 | 3049 | null |

**最终实测（全量生效 + 新带宽，sim_grade_refit 复跑）：**
童生 4.7% / 秀才 9.6% / 举人 12.9% / 进士 13.1% / 探花 15.3% / 榜眼 18.0% / 状元 13.2% / 翰林 8.3% / **文宗 4.9%**。

→ 九档全活、轻底满中尖顶，文宗稳定可达的精英档（~5%）。分布右移但排名健康度未损。

---

## 四、改动清单（可回滚，纯数据/局部代码）

| 文件 | 改动 |
|---|---|
| `config/grades.json` | 专精档 score 150→210（×3，仅 shixian 生效）；九档带宽按组合分布重拟合 |
| `config/inspiration.json` | 新增 `quizCorrectInsp: 1` |
| `js/engine/game.js` | `doQuiz` 知识答对/抉择有效各 `addInspiration(quizCorrectInsp ?? 0)`（`?? 0` 默认关） |

---

## 五、Playtest 失败信号（先定义再开测）

1. **封笔率 > 3%** → 灵感经济过紧（当前 0.2%，远低于线，安全）。
2. **文宗达成率 > 8% 或 = 0** → 顶端带宽仍错位（当前 4.9%，健康）。
3. **榜眼 > 30%** → 中段过载（当前 18%，健康）。
4. **任意主动天赋胜率差 > 15pp** → 超模（重点盯修复后的骰子卡）。
5. **school 选择对终品级相关性 ≈ 0** → P1-1 未解决（本次已让专精成最高流派档，应转正相关）。

---

## 六、未决 / `[PLACEHOLDER]`

- **专精 68% 过高**：流派分区分度偏低。若要让门派稀缺，上调 `attrMin`/`winMin`（P1-1 延伸）。
- **真实分布校准**：仿真用 stub UI（抉择恒有效、支线按 `insp≥cost+8` 自动进），真实玩家抉择/支线离散度不同，上线后需按实机分布微调带宽（本文件数值均为 `[PLACEHOLDER · 附验证路径]`）。
- 不承诺"会火"——只承诺循环好玩 + 每条数值有验证路径。
