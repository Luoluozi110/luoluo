# 飞花棋 NPC「招牌＋破绽＋意图」机制 · 实施路线图

> 状态：待用户确认后开工
> 依据：已完成勘察（playable 引擎/UI/图鉴/存档/仿真 + 编辑器），方案依据七章评审稿（stage-01~07）
> 范围：把七章设计落到 `feihuaqi-playable/`（游戏端）与 `feihua-editors/`（编辑器），含仿真校准与部署
> 铁律：对 playable **增量修改**，严禁用 `extracted/flyhua/feihuaqi/` 整体覆盖（会丢调优补丁与 quality.js）

---

## 一、勘察结论（已完成）

| 关键点 | 现状 | 落地提示 |
|---|---|---|
| 引擎 `game.js` | `pickNpc` 档内随机抽具名；`createSession` 建战斗会话；`resolveBattle` 算分；`settleBattle` 奖惩 | 在 **pickNpc** 挂载机制档案，在 **createSession** 建单场机制会话，在 **resolveBattle** 插入招牌/破绽/意图结算，在 **settleBattle** 记录认知 |
| 规则层 `rules.js` | 纯函数；`pickNpcStyle`/`pickNpcManner` 决定 NPC 行动；`battleScore` 支持 pctMods | 新增 NPC 机制纯函数（意图候选/权重、招牌判定、破绽判定），保持可单测可仿真 |
| 图鉴 `codex.js`(引擎) + `codex.js`(UI) | 四级认知需扩展；目前 `hasFoe/recordFoe/getFoeStats` | 加 `getFoeLevel/recordFoeCognition`；UI 对手详情加分页展示招牌/破绽/意图 |
| 存档 `save.js` | STATE_KEYS 白名单，仅存 `s` | 加：本场机制会话（临时，不必长期存）、图鉴认知（已由 codex 存 localStorage） |
| 战斗 UI `battle.js` | 六步流程，`panel` 逐步渲染 | 第①遭遇插研判卡、第⑤掷骰显示响应、第⑥算分明细、`showVerdict` 加复盘 |
| 配置 `npcs.json` | 具名 NPC 仅 name/title/style/attrs | 逐个加稳定 `id` + `signature`/`weakness`/`intent`（可选，旧档兼容） |
| 编辑器 `npc.js`/`seed-npcs.js` | 编辑/校验/导出 npcs | 加机制字段编辑+校验；seed-npcs 加机制样例 |
| 仿真 `sim_*.mjs` | 用 rules+Game+makeUI 批量跑 | 仿 sim_synergy，新增 npc_mechanism 仿真测第七章指标 |

---

## 二、实施阶段划分（每阶段有可验证产物，逐段确认推进）

### 阶段 A：数据与引擎闭环（对应七章 L1/L2，P0 内核）
**目标**：游戏能"载入机制、生成意图、触发招牌、判定破绽、进结算、记录认知"，先不追求界面美。

1. **A1 配置扩展**：`npcs.json` 每个具名 NPC 加稳定 `id`；新增 `config/npc-mechanics.json`（模板库：8 类招牌/8 类破绽/6 类意图 + 预算旋钮）。为第二章平衡预算提供统一旋钮位。
2. **A2 规则层**：`rules.js` 新增 NPC 机制纯函数模块：
   - 意图候选/权重/锁定（尊重属性+相性+招牌偏好+历史）；
   - 招牌触发判定与效果；
   - 破绽可达性/判定/对招牌的作用；
   - 结算顺序（招牌原始→破绽→招牌实际→并入现有分）。
3. **A3 引擎接线**：`game.js`
   - `pickNpc` 返回带 `mech` 的 NPC（缺失机制 → 旧行为）；
   - `createSession` 建单场机制会话（锁定意图、确定破绽、初始化状态）；
   - `resolveBattle` 按 L1 顺序插入机制修正并输出结构化结果明细；
   - `settleBattle` 记录意图兑现/破绽利用 → 图鉴认知升级。
4. **A4 图鉴认知引擎**：`codex.js` 引擎层加四级认知（未识/相识/察意/破招）+ 破绽成功次数 + 有限历史摘要，跨局持久化。

**验收（A）**：写断言脚本验证——无机制 NPC 行为与旧版一致；教学 NPC（周小满）意图锁定、词体击破招牌生效、结算明细含「招牌·/击破·/意图识破」标签。

> 阶段 A 交付后暂停，供你确认"引擎内核对、数据可配"，再进 UI。

### 阶段 B：交互与反馈（对应七章 L4）
**目标**：玩家看得懂、能执行、有反馈。

5. **B1 研判卡**：`battle.js` 遭遇阶段插入"对手研判"卡（招牌名+效果、破绽线索、意图+可信度、认知等级、详情入口）。
6. **B2 定策提示**：选文体/文风时，接近满足破绽的选项标"可能击破"；追加骰/主动文心显示是否触发响应型招牌。
7. **B3 结算明细**：第⑥按现有五项弹出，补"招牌·/击破·/意图识破"行；战后 `showVerdict` 加简短复盘（意图判断对错、破绽是否抓住、收益来源）。
8. **B4 图鉴 UI**：`codex.js`(UI) 对手详情页加"掌握等级 + 招牌/破绽/意图倾向"，验收分级展示。
9. **B5 殿试递进**：主考官三场跨场意图 + 评语。

**验收（B）**：可用性冒烟（手动/脚本）：首遇可复述招牌+破绽方向；破绽触发有反馈；研判卡不拖慢节奏。

> 阶段 B 交付后暂停，供你确认"界面交互可玩、反馈可懂"。

### 阶段 C：配置内容与编辑器（对应七章 L3/L5 内容侧）
**目标**：正式 NPC 齐全 + 编辑器可编辑校验。

10. **C1 正式 NPC 配置**：按第五章示例，为童生/秀才/举人/进士各配 1~2 名机制 NPC（周小满、林清斋、范解元、苏明哲、欧阳翰、宇文渊、王侍郎），其余保留旧行为。
11. **C2 编辑器**：`npc.js` 加招牌/破绽/意图模板下拉 + 参数表单 + 可达性校验 + 复杂度预算校验；`seed-npcs.js` 同步机制样例；导入旧 NPC 默认旧行为。
12. **C3 云端/导入**：确保导出 `npcs.json` 完整含机制字段；游戏 `normalize` 兜底缺失字段。

**验收（C）**：编辑器导出→游戏载入一致；旧 NPC 导入无报错且保持旧行为；机制 NPC 校验通过。

### 阶段 D：异常与兼容（对应七章第六章）
13. **D1 E0-E4 等级与降级**：`game.js` 汇总异常处理（模板缺失→整套降级旧行为；破绽不可达→备用/规避/关招牌；意图锁定后被改→E0 阻断；重复扣费→资源保护）。
14. **D2 幂等与存档**：读档/刷新不重抽意图、不重复结算；本场机制会话默认空状态兼容旧档。

**验收（D）**：异常用例断言通过（无机制/意图锁定失败/破绽不可达/重复点击）。

### 阶段 E：仿真校准与部署（对应七章 L5 + 上线）
15. **E1 仿真**：`sim_npc_mechanism.mjs`：五策略组（最高即时分/读意图/规划破绽/资源保守/资源激进），测最高属性选择率、破绽利用率、利用胜率差、规划领先、封笔率、品级分布。
16. **E2 调参**：校准招牌 5–10%、破绽摆幅 8–15%、选择率 ≤60%、规划领先 ≥10 个百分点、封笔率 15% 目标。
17. **E3 回归冒烟**：无机制/单机制/复合/殿试全流程语法+行为冒烟（复用现有 verify 思路）。
18. **E4 部署**：`feihuaqi-playable/` 增量改动后，重新部署 GitHub Pages + CloudStudio；编辑器同步。

**验收（E）**：第七章 L5 阈值全部达标；回归无破坏；线上可访问。

---

## 三、资源与产出

| 资源 | 说明 |
|---|---|
| playable 引擎/UI/图鉴/存档 | 增量修改（A3/A4/B/D1/D2） |
| config/npc-mechanics.json + npcs.json | 新增/扩展现有配置（A1/C1） |
| rules.js 新函数 | NPC 机制纯函数（A2） |
| codex（引擎+UI） | 四级认知（A4/B4） |
| battle.js | 研判/提示/明细/复盘（B1-B3/B5） |
| editors npc.js/seed-npcs.js | 编辑校验（C2/C3） |
| sim_npc_mechanism.mjs | 校准（E1/E2） |
| 部署脚本 | 复用 deploy_github2.mjs / cloudstudio（E4） |

## 四、时间节点（估算，实际以交付为准）

- A：引擎内核闭环（本轮起，最大头）
- B：交互反馈
- C：配置内容+编辑器
- D：异常兼容
- E：仿真+部署

每阶段我在交付后暂停待你确认，避免一次性大改无法回溯。

---

## 五、待你确认

1. 是否按 **A→B→C→D→E** 顺序推进？还是调整优先级（如先 C 配内容再 A 挂引擎）？
2. 首期内容范围：是否按第七章建议，"6 名机制 NPC、6 类机制题型"先行，其余旧行为？
3. 每阶段交付后是否都暂停待你确认？还是可在多个子阶段连续执行、只在 A/B/E 大节点暂停？
4. 是否有必须先做的单项？例如你先想看"引擎层闭环+周小满一个教学 NPC"的最小可玩，再扩展。

> 确认后我从阶段 A 开工，每个子步骤会持续跟踪、报进度、及时解决偏差。
