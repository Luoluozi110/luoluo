# 飞花棋·天象策略化改造方案

## 一、结论先行

当前天象系统已经具备“临时状态”框架，但策略存在感偏弱：天象主要在后台直接修改属性、答题收益、战斗奖惩或恢复规则，玩家看见效果后，通常不需要改变路线、章法、文体或资源分配。

建议采用 **“被动底座 + 应势决策 + 可预告窗口”** 的轻改造方案：

1. 保留现有天象的基础效果，避免破坏已有平衡与存档。
2. 每张天象在触发时增加一个一次性的“应势选择”，选择不替换天象本体，而是决定本次天象如何服务当前策略。
3. 选择只在少数关键节点兑现，避免每回合弹窗；玩家可以根据当前路线、章法、灵感和下一场论战做取舍。
4. 通过日志、HUD 角标和“剩余回合/待兑现”提示，让天象从随机 buff 变成可读、可计划的短期环境。

推荐的核心体验是：

> 天象不是“抽到就自动加数值”，而是“天地给出一个窗口，你决定顺势而为，还是借势改局”。

---

## 二、现状诊断

当前 `config/sky.json` 共 6 张天象：

| 天象 | 当前效果 | 当前问题 |
|---|---|---|
| 月圆之夜 | 全员思力临时 +20% | 只影响结算属性，不影响玩家选择 |
| 寒窗雪 | 基本功获得量 +1 | 价值稳定但缺少时机判断 |
| 科场风起 | 论战胜负奖惩 ×2 | 玩家会自然接受，缺少“打不打、何时打”的决策 |
| 文曲星动 | 答题额外收益 | 只影响答对后的收益 |
| 梅雨愁绪 | 平韵格不恢复灵感 | 是被动惩罚，玩家只能承受 |
| 金榜题名时 | 下一场论战得分 +10% | 已有窗口感，但只有一个自动兑现点 |

代码层面已经有较好的扩展基础：

- `Game.doSky()` 负责随机抽取并写入 `s.sky` 或 `s.nextBattlePct`。
- `Game.tickSky()` 负责回合倒计时。
- `effectiveAttrs()`、`doPing()`、`doQuiz()`、`resolveBattle()` 等位置已经按效果类型读取天象。
- 存档层 `save.js` 已按天象 ID 序列化和重新关联，适合继续扩展效果字段。
- `abilityState.strategy` 已有“徐行拾句 / 留白养气 / 换韵生新”三种章法，并具备 `setNextStrategyPlan()`、`consumeStrategyPlan()` 等入口。

因此，本方案建议扩展现有天象契约，不另起一套独立资源系统。

---

## 三、目标与非目标

### 目标

- 让玩家在看到天象后，至少有一次值得思考的取舍。
- 让天象与现有三功、章法、三种文体和棋盘路线发生关系。
- 让天象形成短期计划窗口，而非瞬时数字跳动。
- 保持天象的诗意叙事和轻量反馈，不把游戏变成复杂天气模拟器。
- 兼容现有存档：旧天象仍能读档，新字段缺省时回退旧行为。

### 非目标

- 不把天象变成长期成长系统。
- 不让天象直接替玩家做路线或战斗选择。
- 不引入天气相克、复杂元素克制、天气叠层等高维护机制。
- 不让天象效果超过文心、流派和三功系统的主导地位。

---

## 四、推荐机制：应势决策

### 4.1 天象卡新增字段

在现有效果之外，为天象增加可选的 `choices` 字段。示例：

```json
{
  "id": "SK01",
  "name": "月圆之夜",
  "turns": 6,
  "scope": "all",
  "effect": {
    "type": "attr_pct",
    "attr": "si",
    "value": 0.2
  },
  "choices": [
    {
      "id": "observe",
      "name": "借月养思",
      "desc": "本阶段章法为「留白养气」时，首次成功答题额外获得 1 构思。",
      "effect": { "type": "sky_strategy", "key": "strategy_on_quiz", "value": 1, "plan": "guard" }
    },
    {
      "id": "advance",
      "name": "趁月疾行",
      "desc": "下一次移动前，可将 1 构思转为额外移动 1 格；每个天象至多一次。",
      "effect": { "type": "sky_strategy", "key": "strategy_move_plus", "value": 1, "cost": 1 }
    }
  ]
}
```

建议不要把选择写成永久独立状态。只在 `s.sky` 条目上记录：

```js
{
  card,
  left: 6,
  choiceId: "observe",
  triggered: false
}
```

这样读档只需继续保留原有 `card.id + left`，新增字段缺失时按旧行为处理。

### 4.2 选择时机

推荐在天象展示弹窗中直接选择，不增加二次打断：

1. 落到天象格。
2. 展示天象叙事和基础效果。
3. 下方显示 2 个“应势方向”与 1 个“顺其自然”。
4. 玩家选择后天象进入 HUD。
5. 选择只影响本张天象，不改变其基础效果。

“顺其自然”不是空选项：它代表不绑定额外策略，适合玩家不想承担条件或暂时没有资源的情况。

### 4.3 触发原则

应势效果必须满足三条：

- **有条件**：例如首次答题、下一次经过某类格、下一场论战。
- **有上限**：每张天象最多触发 1～2 次。
- **可观察**：触发前在 HUD 显示“待兑现”，触发后显示日志。

不要做“每回合自动 +1 构思”这种无脑收益，它会把天象变成隐形工资，而不是策略窗口。

---

## 五、六张现有天象的具体改造

以下数值是第一版建议值，原则是“每张天象额外带来的策略价值约等于半次小型章法，不超过一次完整章法”。

### SK01 月圆之夜：养思或趋行

保留：思力临时 +20%。

增加二选一：

- **借月养思**：本阶段章法为“留白养气”时，首次答对知识题或完成创作抉择，构思 +1；每张天象 1 次。
- **趁月趋行**：下一次进入仄韵格或奇遇格时，若构思 ≥1，可消耗 1 构思，额外获得 1 格移动；每张天象 1 次。

策略意义：玩家要判断自己是准备答题积累，还是利用路线冲击关键格。

### SK02 寒窗雪：磨基本功或蓄稿

保留：基本功获得量 +1。

增加二选一：

- **闭门苦读**：下一次仄韵格基本功成长结算后，额外获得 1 稿页；每张天象 1 次。
- **雪夜成篇**：下一场论战若使用本阶段当前熟练度最高的文体，胜利后额外获得 1 残页；每张天象 1 次。

策略意义：把“基本功收益”连接到笔力资源或文体选择，而非只看属性上涨。

### SK03 科场风起：押注论战或稳住资源

保留：论战胜负奖惩 ×2，但建议明确提示该效果包含损失与收益。

增加二选一：

- **迎风入场**：下一场论战开始时，若构思 ≥1，可消耗 1 构思，使本场得分再 +8%；胜负奖惩仍按原规则结算。
- **避风收笔**：放弃本张天象的战斗加码，改为本阶段第一次论战失败时少损失 2 点灵感；每张天象 1 次。

策略意义：玩家可选择高风险搏高收益，或把强天气转成失败保护，尤其适配“留白养气”和低灵感状态。

平衡说明：不建议让“迎风入场”同时翻倍奖励再叠加超过 10%，第一版先用 8%，并设置每张天象仅一次。

### SK04 文曲星动：专攻考题或转化为章法

保留：答题答对额外收益 +1。

增加二选一：

- **星照题眼**：下一道知识题答对时，额外获得 1 心得；每张天象 1 次。
- **星移笔锋**：下一次创作抉择结算后，可将本次获得的 1 心得改为对应目标属性研修进度 +1；每张天象 1 次。

策略意义：玩家根据当前研修焦点，在“资源上限”与“定向成长”之间选择。

### SK05 梅雨愁绪：改走路线或主动破雨

保留：平韵格不恢复灵感。

增加二选一：

- **避雨改韵**：下一次移动落入平韵格前，若构思 ≥1，可消耗 1 构思，将该格按仄韵格结算；每张天象 1 次。棋子仍停在原格，只有结算类型改变。
- **雨中磨墨**：接受平韵格的“灵感不恢复”，但下一次平韵格不恢复时获得 1 残页；每张天象 1 次。

策略意义：负面天象终于提供反制与取舍，不再只是被动惩罚。

限制：第一版不要允许改成考题格、奇遇格或战斗格，只允许在平韵与仄韵之间转换，减少规则歧义。

### SK06 金榜题名时：强攻或保留

保留：下一场论战得分 +10%。

增加二选一：

- **一鼓作气**：本场若取胜，额外获得 1 构思；若失败，不返还任何资源。
- **藏锋待发**：将 +10% 延迟至下一场论战，但必须在下一场使用与当前不同的文体；每局最多延迟一次。

策略意义：直接形成“现在打还是等更好对手/更合适文体”的判断。

实现建议：不增加新的 `sky` 卡，只把 `nextBattlePct` 从数字扩展为可选的待兑现记录，兼容旧值：

```js
nextBattlePct: 0,
nextBattleSky: null
```

---

## 六、与现有三功和章法的接法

### 6.1 不新增资源

优先复用现有资源：

- 思力属性 `si`
- 心得 `abilityState.insight`
- 构思 `abilityState.strategy.charges`
- 稿页/残页 `abilityState.manuscript`
- 当前阶段 `s.phase`
- 当前章法 `abilityState.strategy.plan`
- 当前文体 `session.lastStyle` 或战斗选择 style

天象只增加“触发条件”和“消费点”，不新增“天气能量”“星象值”等新资源。

### 6.2 建议新增的最小引擎接口

```js
skyChoice(card, choiceId)
skyCanTrigger(key, ctx = {})
consumeSkyTrigger(key, ctx = {})
activeSkyChoice(key, ctx = {})
```

建议全部集中在 `game.js`，而不是把条件散落到 `doQuiz()`、`doPing()`、`resolveBattle()` 的多个匿名判断里。

伪代码：

```js
skyCanTrigger(key, ctx = {}) {
  return this.s.sky.some(item => {
    const choice = item.choice || {};
    const effect = choice.effect || {};
    return effect.key === key
      && !item.triggered
      && this.skyContextMatches(item, ctx);
  });
}

consumeSkyTrigger(key, ctx = {}) {
  const item = this.s.sky.find(item =>
    !item.triggered && (item.choice?.effect?.key === key) && this.skyContextMatches(item, ctx)
  );
  if (!item) return false;
  item.triggered = true;
  // 在此处统一扣构思、发放稿页或写入战斗会话标记
  return true;
}
```

### 6.3 不建议的实现方式

- 不要在 `skyActive(type)` 上继续堆几十种特殊 type；该方法适合无条件被动效果，不适合有条件的一次性决策。
- 不要把选择直接改写成文心效果；天象应当是环境窗口，不能污染文心持有和升级逻辑。
- 不要让天象直接覆盖玩家已选章法；它可以奖励某章法，不能替玩家强制换章法。

---

## 七、UI 与信息呈现

### 7.1 天象弹窗

现有 `showSky(card)` 已有天象图标、叙事和基础效果展示。建议扩展为：

- 基础效果：始终显示。
- “应势一择”：显示 2 个策略选项 + “顺其自然”。
- 每个选项显示：触发条件、资源消耗、预计收益、次数上限。
- 玩家选完后才关闭弹窗。

文案示例：

> 月圆之夜已临。清辉能照见诗句，也照见你的取舍。
>
> 借月养思：本阶段首次答题成功，构思 +1。
>
> 趁月趋行：下一次进入仄韵格或奇遇格时，可消耗 1 构思多行 1 格。
>
> 顺其自然：只承受月圆之夜的基础效果。

### 7.2 HUD

现有天象角标显示名称和剩余回合。建议追加：

- 已绑定选择：`月圆之夜 · 借月养思`。
- 待兑现状态：金色点或文字“待应势”。
- 已触发状态：变为灰色“已应势”，直到天象结束。
- 一次性天象：继续使用“下一场论战 +10%”角标，但补充“可延迟/已锁定”提示。

### 7.3 战报

统一使用可检索日志：

- `天象·月圆之夜：选择「借月养思」`
- `天象·借月养思：首次答题成功，构思 +1`
- `天象·梅雨愁绪：避雨改韵，平韵格按仄韵格结算`

这样玩家能在回看战报时理解天象为何产生影响。

---

## 八、存档与兼容

### 8.1 推荐存档结构

当前天象存档使用 ID 和剩余回合重新关联。扩展后可保持兼容：

```json
{
  "sky": [
    {
      "id": "SK01",
      "left": 4,
      "choiceId": "observe",
      "triggered": false
    }
  ],
  "nextBattlePct": 0,
  "nextBattleSky": null
}
```

### 8.2 旧档规则

- 旧天象没有 `choiceId`：视为“顺其自然”，基础效果继续生效。
- 旧档 `sky` 条目仍是 `{ id, left }`：`deserializeRun()` 自动补 `choiceId: null, triggered: false`。
- 旧档只有 `nextBattlePct`：继续按旧逻辑结算，不尝试补造新的延迟选择。
- 天象配置删卡：沿用当前行为，读档时移除无效卡并写入 warning。

### 8.3 保存时机

选择天象后应立即：

1. 更新 `s.sky`。
2. `ui.onState(s)`。
3. 调用 `onForceSave?.()`。

每次一次性应势效果兑现后也立即保存，避免玩家刷新页面后重复获得。

---

## 九、分阶段实施建议

### 第一阶段：低风险增强

只做“选择 + HUD + 存档字段”，不改大范围结算：

- 6 张天象全部加入 2 个应势选项。
- 选择记录在 `s.sky`。
- 天象弹窗和 HUD 显示绑定策略。
- 新增统一 `skyCanTrigger / consumeSkyTrigger`。
- 先落地 SK01、SK03、SK05 三张卡的触发逻辑。

验收重点：玩家能看懂、能选择、选择能保存，旧档不坏。

### 第二阶段：策略兑现

补齐 SK02、SK04、SK06，并把触发点接入：

- `doQuiz()`：答题/创作抉择触发。
- `doPing()`：平韵转仄韵或雨中磨墨触发。
- 移动结算：路线窗口触发。
- `resolveBattle()`：战斗前后强攻、延迟和奖励触发。

### 第三阶段：平衡与内容打磨

- 统计每张天象的选择率、触发率、兑现收益。
- 若某选项选择率低于 20%，优先调整描述和触发条件，再调整数值。
- 若天象造成单局额外得分超过约 8%～10%，降低数值或限制触发次数。
- 对梅雨愁绪重点观察负面体验，确保玩家有可行反制但不能完全无成本解除。

---

## 十、测试与验收标准

建议新增 `tests/sky-strategy.test.mjs`，至少覆盖：

### 数据层

- 所有天象 ID 唯一。
- 每张天象 `choices` 至少有两个策略选项，字段完整。
- `choiceId` 非法时回退为顺其自然。
- `triggered` 非布尔值时归一化为 false。

### 引擎层

- 选择天象后基础效果仍生效。
- 选择一个应势方向后，另一方向不会触发。
- 一次性效果第二次调用不会重复发奖。
- 构思不足时不应消耗、不应标记为已触发。
- 天象结束后，待触发状态一并失效。
- 重复进入相同天象时，按现有规则刷新时长，但不复制无限次策略收益。
- 梅雨愁绪改韵只能改变结算类型，不改变棋子位置。
- `nextBattlePct` 旧值和 `nextBattleSky` 新值都能正常结算。

### 存档层

- 新档保存并读回 `choiceId / triggered`。
- 旧格式 `{ id, left }` 能读档，且按顺其自然继续。
- 存档读回后不能重复触发已经消费的应势效果。
- 无效天象 ID 仍按现有 warning 机制清理。

### UI 层

- 天象弹窗显示基础效果和策略选项。
- 点击任一选项只返回一个合法 choiceId。
- 点击“顺其自然”返回 null，不阻塞游戏。
- 选择后 HUD 能显示名称、策略和待触发状态。
- 移动端三选项不会溢出，按钮可点击区域不小于现有弹窗标准。

---

## 十一、最终推荐

建议先实施第一阶段，并优先落地三张最能产生策略张力的天象：

1. **科场风起**：高收益与失败风险之间的押注。
2. **梅雨愁绪**：负面环境下的路线/资源反制。
3. **月圆之夜**：答题成长与路线推进之间的选择。

这三张分别代表：

- 战斗决策
- 路线决策
- 资源与成长决策

如果三张卡在实机中能让玩家主动改变下一步计划，再把同一契约复制到其余三张。这样既能显著提升天象存在感，又不会让系统一次性膨胀成新的复杂子游戏。

**一句话方案：保留天象的自动环境效果，但让玩家在天象初临时选择一种“应势方式”，并在未来 1～6 回合内围绕这个选择安排路线、章法、答题与论战。**
