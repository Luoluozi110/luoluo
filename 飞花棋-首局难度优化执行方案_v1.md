# 飞花棋：首局难度优化执行方案

> 文档版本：v1.0
> 日期：2026-08-31
> 状态：已确认方向，可拆分为开发任务
> 适用范围：单人可玩版 `feihuaqi-playable/`
> 核心目标：降低首次完整游玩的随机难度尖峰，让新手先理解论战、经历可复盘的失败，再进入标准难度。

---

## 一、已锁定决策

以下内容已经确认，本轮执行时不再作为开放问题：

1. 新增仅面向“尚未完成过任何一局”的首局保护，产品名暂定为“入门卷”。
2. 不通过全属性加成、暗改骰子或强制胜利来降低难度。
3. 首局保护重点处理 NPC 抽取、首次晋阶试、首次败北反馈和论战教学信息密度。
4. 入门卷结束后恢复标准规则，不长期削弱正常对局。
5. 涉及答题的内容全部排除：不修改题库、题目难度、题目权重、答题奖励、答题惩罚、倒计时、判定和答题 UI。

### 1.1 明确不在本次范围内

- `config/questions.json` 的任何字段与内容；
- `rules.js` 的 `difficultyWeights()`、`pickQuestion()`；
- `game.js` 的 `doQuiz()`；
- `inspiration.json` 中 `quizWrong`、`quizCorrectInsp`；
- 情境题文案、选项、解析和答题倒计时；
- 全局六维初始值、全局战斗公式和全局评分线；
- 文心、三功、流派和名篇的基础数值重构。

如果实施过程中发现必须修改以上范围才能继续，应停止该部分工作并重新评审，不得顺手扩大范围。

---

## 二、问题证据与设计判断

### 2.1 已知事实

1. 玩家初始六维均为 5，流派主属性再加 3，开局六维总和约为 33。
2. 童生池的多数常规对手六维总和约为 35；`shen_sui_feng` 六维总和为 71。
3. NPC 未配置 `weight` 时，当前规则按默认权重 100 处理，因此池内未配置权重的 NPC 实际等权。
4. 外圈路线 17、27 是普通论战格，玩家可能因骰点跨过；路线 36 的秀才晋阶试是硬关口，必定结算。
5. 秀才池中常规对手六维总和约为 61–63，但 `xie_lian_cheng` 与 `gu_qing_shang` 分别达到 123、117；当前同样没有显式低权重。
6. 论战失败已具备“败中有得”：出战文体 +2；但首次失败时，玩家未必能清楚看见自己获得了什么、下一次该改变什么。
7. 当前 `tutorialState` 在每次 `Game.start()` 时重新创建，它表示本局教学节点是否看过，不等同于“玩家是否完成过首局”。

### 2.2 设计判断

首局劝退的首要风险不是整个数值盘普遍过高，而是以下三项叠加：

```text
可能跳过教学战
    ↓
首次论战直接进入秀才晋阶试
    ↓
等权抽到超规格或复杂机制 NPC
    ↓
玩家尚未建立因果理解便遭遇明显失败
    ↓
把失败归因为随机、不公平或“这游戏不是给我玩的”
```

因此，本方案优先压低首局的随机尖峰，不削弱核心论战公式，也不取消失败。

### 2.3 待验证假设

- 固定首场对手层级后，首场论战胜率可稳定在 75%–85%。
- 首次秀才晋阶试排除超规格精英后，胜率可稳定在 60%–75%。
- 把失败成长显性展示并提供一次轻量资源返还，可显著降低首次失败后的退出率。
- 分段呈现论战信息能缩短首场决策时间，同时不降低玩家对规则的理解。

以上均须通过仿真或真人试玩确认，不视为已经达成。

---

## 三、目标体验与验收方向

### 3.1 核心体验

新手在首局应依次获得三种感受：

1. **我看得懂**：知道文体、文风和灵感骰分别在影响什么。
2. **我的选择有效**：胜负结果能对应到刚才的选择，而非只看到一串总分。
3. **失败仍有进展**：首次败北后明确变强，并知道下一场可以如何调整。

### 3.2 一句话规则

玩家在入门卷中通过受控的对手梯度学习论战，在保留真实胜负和资源代价的前提下，逐步过渡到标准难度。

### 3.3 不追求的结果

- 不要求新手首局必胜或必定通关；
- 不要求首局所有论战都比标准局简单；
- 不把所有进阶 NPC 永久移出正常对局；
- 不用隐藏动态难度实时改写已开始的战斗。

---

## 四、功能规则契约

## 4.1 入门卷判定

### 判定规则

```text
onboarding.enabled = 跨局统计 stats.games === 0
```

- 从未完成过任何结局：开启入门卷；
- 中途退出或存档后继续：仍保持入门卷；
- 完成任意结局并完成跨局结算：后续新局关闭入门卷；
- 载入当前入门卷存档：以存档中的 `onboarding.enabled` 为准，不能因外部统计变化而中途切换；
- 老玩家旧存档没有 `onboarding`：根据跨局完成局数补齐；已有完成记录时默认关闭。

### 建议状态结构

```js
onboarding: {
  version: 1,
  enabled: true,
  disabledByPlayer: false,
  battleCount: 0,
  lossAidUsedByPhase: {},
  firstBattleCompleted: false,
  firstGateCompleted: false
}
```

### 玩家开关

- 开局说明或设置中提供“关闭入门提示与保护”；
- 关闭后立即设置 `disabledByPlayer = true`，本局后续按标准规则运行；
- 不提供战斗进行中的临时开启或关闭，避免改变已锁定的对手与结算；
- 设置项文案必须明确：关闭后只影响之后的论战，不追溯重算已发生结果。

### 排行榜与跨局奖励

建议规则：

- 入门卷保留文心、图鉴、流派熟练度、名篇和其他正常跨局成长；
- 入门卷结算记录增加 `onboardingRun: true`；
- 若成绩会上传竞技排行榜，则入门卷成绩不进入竞技榜，结算页明确说明；
- 本地历史记录可正常保留，避免玩家觉得首局被作废。

若当前排行榜不存在入门卷过滤能力，此项可以独立延期，但上线前必须确认是否会造成榜单公平性问题。

---

## 4.2 NPC 数据分层

### 新增配置字段

建议在具名 NPC 条目上增加：

```json
{
  "difficultyRole": "tutorial",
  "beginnerWeight": 100,
  "standardWeight": 100
}
```

字段含义：

| 字段 | 可选值 | 用途 |
|---|---|---|
| `difficultyRole` | `tutorial` / `basic` / `advanced` / `elite` | 表示教学复杂度和数值定位 |
| `beginnerWeight` | 非负数 | 入门卷普通遭遇权重；0 表示不进入入门卷随机池 |
| `standardWeight` | 非负数 | 标准局权重；缺省时兼容现有 `weight` |

兼容顺序建议：

```text
入门卷：beginnerWeight → weight → 默认权重
标准局：standardWeight → weight → 默认权重
```

### 首批标注原则

- 童生级现有 `complexity: tutorial` 的 NPC 标为 `tutorial`；
- 机制简单、数值接近档位基准的 NPC 标为 `basic`；
- 机制需要跨场记忆、反复换策或额外研判的标为 `advanced`；
- 六维预算显著高于同档中位数的标为 `elite`；
- `shen_sui_feng` 应标为 `elite`；
- `xie_lian_cheng`、`gu_qing_shang` 应标为 `elite`。

### 权重建议

第一版先使用保守值：

| 定位 | `beginnerWeight` | `standardWeight` |
|---|---:|---:|
| tutorial | 100 | 100 |
| basic | 100 | 100 |
| advanced | 20 | 100 |
| elite | 0 | 15 |

`standardWeight` 是否将高级机制从当前 100 调低到其他值，属于全局平衡决定。首版若要严格控制影响面，可让 advanced 保持 100，只把明显超规格的 elite 设为 15。

---

## 4.3 首局论战梯度

### 遭遇选择规则

入门卷开启时：

| 节点 | 候选范围 | 限制 |
|---|---|---|
| 第 1 场普通论战 | 当前阶段的 `tutorial` | 必须有公开招牌与可执行破绽；不得使用跨场机制 |
| 第 2 场普通论战 | 当前阶段的 `tutorial` 或 `basic` | 不得选 `elite` |
| 第 3 场及以后普通论战 | 按 `beginnerWeight` 抽取 | `elite` 权重为 0 |
| 首次秀才晋阶试 | 秀才级 `basic` | 排除 117/123 的超规格对手 |
| 后续晋阶试 | 所属档位按 `beginnerWeight` 抽取 | 不选 `elite`；保留 advanced 的低概率 |
| 殿试 | 沿用当前主考规则 | 不做战斗内动态削弱 |

### 边界情况

1. 若目标池没有符合条件的 NPC，按以下顺序放宽：

   ```text
   tutorial → basic → advanced → 当前原始池
   ```

   每次放宽必须写入开发日志，便于发现配置缺口。

2. 阶段必遇规则与入门卷冲突时：

   - 入门卷前两场和首次秀才晋阶试优先于普通 `stageForcedWhen`；
   - 构筑承诺型的终局或殿试必遇规则不受影响；
   - 被推迟的阶段必遇 NPC可在该阶段下一场符合条件的普通论战恢复触发。

3. 支线高潮替换主线对手时：

   - 不替换支线角色身份；
   - 沿用现有支线预算投影逻辑；
   - 入门卷只限制投影目标不能来自 `elite` 档预算。

4. 续玩存档如果已经生成战斗会话，不重新抽取对手；入门规则只作用于下一场尚未生成的战斗。

### 推荐实现接口

在 `npc-selection.js` 增加纯函数：

```js
export function npcWeightForMode(npc, onboardingEnabled) {}
export function poolForOnboarding(game, tier, context) {}
```

`context` 至少包含：

```js
{
  isGate: false,
  phase: 'child',
  battleCount: 0,
  isFirstXiucaiGate: false,
  isPalace: false
}
```

`pickNpcFromTier()` 只负责调用筛选与权重函数，不在函数体中硬编码 NPC 姓名。

---

## 4.4 首次败北点拨

### 触发条件

同时满足以下条件时触发：

- `onboarding.enabled === true`；
- 本场结果为 `lose`；
- 本阶段尚未使用点拨：`lossAidUsedByPhase[phase] !== true`；
- 本场不是殿试、隐藏终卷或已经标记不允许救济的特殊战；
- 战斗已经正常结算，非异常中断或重复结算。

### 数值效果

第一版采用最小补偿：

```text
返还灵感 2
```

该返还只抵消当前早期配置中的败北额外损失，不返还应战成本、追加骰成本、主动文心成本或其他主动消费。

现有结果仍全部保留：

- 失败记录照常 +1；
- 战绩评分照常按失败计算；
- “败中有得”的文体成长照常结算；
- 心得、熟悉度和文心触发照常结算；
- NPC 机制历史照常写入。

### 结算顺序

```text
正常战斗结算
→ 失败损失
→ 失败成长与心得
→ 判断是否触发首次败北点拨
→ 灵感返还 2
→ 展示复盘卡
→ 保存安全快照
```

### 复盘卡内容

复盘卡只使用玩家已经看见的信息，不泄露隐藏意图或未触发效果：

```text
师友点拨

此战虽败，你已经有所领悟：
诗力 +2 · 心得 +2

本场主要失分：未命中对手公开破绽。
下次可先看对手研判，再选择文体与文风。

师友替你补回灵感 2。
```

按钮：`记下了，继续赶考`

如果系统无法可靠生成“主要失分”，则降级为确定事实：

```text
你已从败局中获得成长。下一场先查看公开破绽，再决定是否追加灵感骰。
```

不得使用“你选错了”“最佳答案是……”或基于隐藏信息的唯一解文案。

---

## 4.5 首场论战信息降噪

### 原则

首场仍保留完整的遭遇、审题、选文体、选文风、掷灵感骰和算分流程，但把一次性六步长说明改为“当前节点一条提示”。

### 首场显示规则

| 节点 | 必须展示 | 暂不主动展开 |
|---|---|---|
| 遭遇 | 对手招牌、破绽、下一步按钮 | 机制模板术语、跨场历史 |
| 选文体 | 三体格律预估、推荐标签及理由 | 完整公式推导 |
| 选文风 | 本题相性、风潮修正、公开破绽命中情况 | 所有未选文风的长解释 |
| 灵感骰 | 当前灵感、追加成本、预计新增效果 | 文心和羁绊的完整内部计算 |
| 结算 | 最大优势、主要失分、成长结果 | 默认折叠完整逐项公式 |

推荐标签示例：

```text
推荐：诗体
理由：你当前诗力最高，且可命中对手公开破绽。
```

推荐只提供解释，不锁定选项、不自动确认，也不保证选择后一定胜利。

### 后续场次

- 第二场保留推荐标签，但不再自动弹出节点说明；
- 第三场起默认隐藏推荐标签，只保留玩家主动点击的“为什么”；
- 玩家关闭入门卷后，从下一场开始立即按标准展示；
- 完整六步说明继续保留在规则页，不删除已有规则内容。

---

## 五、具体文件改动清单

## 5.1 配置与数据

### `feihuaqi-playable/config/npcs.json`

- 为所有具名 NPC 补 `difficultyRole`；
- 为首批童生、秀才 NPC 补 `beginnerWeight` 与 `standardWeight`；
- 将三个已识别超规格 NPC 标为 `elite`；
- 不修改 NPC 六维、招牌、破绽和意图数值。

### `feihuaqi-playable/config/narrative.json`

新增或扩展：

```json
{
  "tutorial": {
    "onboardingRun": {},
    "battleSteps": {},
    "lossAid": {}
  }
}
```

只存显示文案，不把灵感返还值、权重和判定条件写进叙事配置。

### `feihua-content.json`

- 如果正式云端内容包覆盖 `npcs` 或 `narrative`，必须同步本次字段；
- 同步后验证本地配置与云端内容包中的 NPC 入门字段一致；
- 禁止只改本地 `config/`，否则线上可能被云端工程覆盖回旧数据。

## 5.2 引擎

### `feihuaqi-playable/js/engine/game.js`

- `start()`：创建 `onboarding` 状态；
- `doBattleCell()`：向 NPC 选择器传入普通战、晋阶试、阶段和战斗序号；
- `settleBattle()`：在正常失败结算后执行一次性点拨；
- 战斗结束后递增 `onboarding.battleCount`；
- 首次秀才晋阶试结束后设置 `firstGateCompleted`；
- `endGame()`：向结算与排行榜数据写入 `onboardingRun`；
- 不修改 `doQuiz()`。

### `feihuaqi-playable/js/engine/npc-selection.js`

- 新增入门池筛选和模式权重函数；
- `pickNpcFromTier()` 接收可选 `context`；
- 标准局缺少新字段时保持当前选择行为；
- 候选池放宽规则必须可单元测试；
- 不硬编码 NPC 显示名。

### `feihuaqi-playable/js/engine/save.js`

- 把 `onboarding` 加入可保存状态；
- 新增 `normalizeOnboardingState()`；
- 旧档迁移时根据跨局完成局数补齐；
- 清洗非法战斗计数、版本号和阶段使用记录；
- 保证读取旧档不报错、续玩不重复领取同阶段点拨。

### `feihuaqi-playable/js/engine/album.js`

- 复用现有 `stats.games` 判断玩家是否完成过对局；
- 不改变 `stats.games` 的结算时机和含义；
- 如现有读取接口不便于引擎使用，只增加只读查询函数。

## 5.3 UI

### `feihuaqi-playable/js/ui/battle.js`

- 首场论战改为节点式提示；
- 显示推荐标签及其确定性理由；
- 结算时支持入门复盘摘要；
- 完整明细继续可展开查看。

### `feihuaqi-playable/js/ui/modals.js`

- 新增“入门卷说明”与“师友点拨”弹窗；
- 增加关闭入门卷的确认说明；
- 不修改答题弹窗及答题结果弹窗。

### `feihuaqi-playable/js/ui/app.js` 或设置菜单模块

- 开局显示一次入门卷状态；
- 设置中增加关闭入口；
- HUD 可显示轻量“入门卷”标记，避免玩家不知道当前规则不同。

## 5.4 编辑器与契约

如果 NPC 编辑器会重新导出 `npcs.json`，必须同步支持：

- `difficultyRole` 下拉选择；
- `beginnerWeight`、`standardWeight` 数值输入；
- 非负数校验；
- 未填写时保持兼容默认；
- 导入、导出和云端发布不丢字段。

配置契约应补充字段类型校验，但不强制所有历史 NPC 立即填写。

---

## 六、任务拆分与推荐顺序

## P0：NPC 数据分层与选择器

### 工作项

1. 为童生、秀才 NPC 标注难度角色和模式权重；
2. 实现 `npcWeightForMode()` 与 `poolForOnboarding()`；
3. 接入首场、第二场和首次秀才晋阶试规则；
4. 加入标准局兼容测试；
5. 同步云端内容包和 NPC 编辑器字段。

### 完成条件

- 入门卷首场不会抽到 advanced/elite；
- 首次秀才晋阶试不会抽到 `xie_lian_cheng`、`gu_qing_shang`；
- 标准局仍可抽到全部合法 NPC；
- 缺少新字段的旧配置可以启动并保持旧行为。

## P1：首败点拨与存档

### 工作项

1. 增加 `onboarding` 存档状态与迁移；
2. 实现每阶段一次的败北灵感返还；
3. 增加复盘卡和降级文案；
4. 验证重复结算、读档和异常中断不会重复返还。

### 完成条件

- 同一阶段最多返还一次；
- 返还恰好为 2，不受奇士正向灵感倍率等被动放大；
- 失败、成长、战绩和 NPC 历史仍正常记录；
- 殿试与隐藏终卷不触发点拨。

## P2：首场论战信息降噪

### 工作项

1. 将首场长说明拆入各决策节点；
2. 实现推荐标签和理由；
3. 实现首场结算摘要；
4. 保留完整规则与算分明细的主动入口。

### 完成条件

- 每个决策页只主动展示一条当前相关教学；
- 推荐理由只使用公开信息；
- 玩家始终可以选择非推荐项；
- 第二场开始不重复弹出首场说明。

## P3：数据记录与灰度

### 工作项

1. 增加入门卷标记和关键事件记录；
2. 排行榜按产品决定过滤入门卷成绩；
3. 用灰度或 A/B 方式验证关键指标；
4. 达标后再决定是否调整 standardWeight。

---

## 七、测试清单

## 7.1 自动化测试

建议新增 `feihuaqi-playable/tests/onboarding-difficulty.test.mjs`，至少覆盖：

### 首局判定

- `stats.games = 0` 时新局开启入门卷；
- `stats.games > 0` 时新局关闭入门卷；
- 入门卷中途存档、读档后仍开启；
- 玩家主动关闭后读档不重新开启；
- 旧存档没有 `onboarding` 时可安全迁移。

### NPC 抽取

- 首场候选只包含 `tutorial`；
- 第二场候选不包含 `elite`；
- 首次秀才晋阶试只包含 `basic`；
- 候选池为空时按规定顺序放宽；
- 标准局仍使用标准权重；
- `weight = 0` 的 NPC 不被误抽中；
- 阶段必遇被推迟后能在后续合法场次恢复。

### 首败点拨

- 同阶段第一次失败返还 2；
- 同阶段第二次失败不返还；
- 新阶段第一次失败可再次返还；
- 胜利和平局不触发；
- 殿试、隐藏终卷不触发；
- 重复调用结算不会二次返还；
- 返还不被流派或文心的正向倍率放大；
- 应战、追加骰和主动文心消费不会被错误返还。

### 范围保护

- `questions.json` 内容哈希或快照不变；
- `difficultyWeights()` 与 `pickQuestion()` 行为不变；
- `doQuiz()` 相关现有测试全部通过；
- 答题奖励、答题惩罚和倒计时保持原值。

## 7.2 配置与内容校验

- 所有 `difficultyRole` 都属于允许枚举；
- 所有新权重为有限非负数；
- 每个阶段至少存在一个非 elite 候选；
- 童生池至少存在一个 tutorial 候选；
- 秀才池至少存在一个 basic 晋阶候选；
- 本地 `npcs.json` 与 `feihua-content.json` 同步；
- NPC 编辑器导入再导出后新字段不丢失。

## 7.3 回归测试

- 全量 `feihuaqi-playable/tests/*.test.mjs`；
- 配置契约测试；
- 存档迁移与存档码测试；
- NPC 机制、阶段必遇、支线高潮与殿试测试；
- 排行榜上传测试；
- JavaScript 语法检查与 `git diff --check`。

当前根目录 `sim_npc_tier_winrate.mjs` 未对当前三圈棋盘补齐 `routeCells` 归一化，直接运行会在移动阶段报错。正式做胜率校准前，应先修复或改用当前配置加载器生成仿真配置；不得引用失败脚本的旧结果作为本次平衡证据。

---

## 八、真人试玩方案

## 8.1 分组

优先使用两轮单变量测试：

### 第一轮：只验证 NPC 梯度

- A 组：当前版本；
- B 组：只启用 P0 的入门 NPC 选择规则；
- 其他教学、资源和界面保持一致。

### 第二轮：验证失败反馈

- 所有玩家使用已验证的 P0；
- A 组：当前失败结算；
- B 组：启用 P1 首败点拨；
- 不同时上线 P2 信息降噪，避免无法判断退出率改善来源。

P2 应在 P0、P1 数据稳定后单独验证。

## 8.2 建议记录指标

| 指标 | 目标 | 失败信号 |
|---|---:|---:|
| 首场论战胜率 | 75%–85% | <65% 或 >90% |
| 首次秀才晋阶试胜率 | 60%–75% | <50% 或 >85% |
| 首次失败后继续至少 3 回合 | ≥80% | <65% |
| 首场论战中途退出率 | ≤10% | >20% |
| 首场论战决策总时长 | 较当前下降 15%–30% | 无下降或下降超过 45% |
| 能说出文体/文风/灵感骰用途的玩家 | ≥70% | <55% |
| 标准局总体胜率变化 | 不超过 ±3 个百分点 | 超过 ±5 个百分点 |

这些数值是验收目标，不是当前实测结论。样本量不足时同时报告人数和原始计数，不只报告百分比。

## 8.3 观察问题

试玩后只问以下四个问题：

1. 你觉得第一场为什么赢或为什么输？
2. 下一场你会改变哪一个选择？
3. 失败后获得了什么？
4. 你是否察觉这是入门规则？它有没有削弱成就感？

若玩家无法回答第 1、2 题，问题仍在反馈结构，不应继续加数值补偿。

---

## 九、埋点与日志建议

建议记录以下事件；若项目暂不接分析平台，可先落本地调试日志：

```text
onboarding_run_started
onboarding_disabled
onboarding_battle_started
onboarding_battle_finished
onboarding_gate_started
onboarding_gate_finished
onboarding_loss_aid_used
onboarding_run_finished
```

关键字段：

```js
{
  runId,
  onboardingRun,
  battleIndex,
  phase,
  isGate,
  npcId,
  npcDifficultyRole,
  playerAttrTotal,
  npcAttrTotal,
  result,
  inspirationBefore,
  inspirationAfter,
  lossAidUsed,
  durationMs,
  quitWithinNextThreeTurns
}
```

不得记录题目正文、答题选项或本方案范围外的答题行为变化。

---

## 十、风险与回退

| 风险 | 表现 | 防护 |
|---|---|---|
| 首局过于简单 | 玩家误判标准局难度 | 不强制胜利；秀才晋阶试仍保留真实压力 |
| 入门卷污染排行榜 | 新手保护成绩挤占标准榜 | 上传时携带 `onboardingRun` 并过滤 |
| 云端配置覆盖本地字段 | 线上恢复等权抽取 | 同步 `feihua-content.json`，部署后验证 raw 内容 |
| 编辑器丢字段 | 内容人员保存后保护失效 | 编辑器导入导出专项测试 |
| 首败返还被倍率放大 | 返还超过 2 | 使用精确状态回补，不走普通正向灵感加成链 |
| 旧档重复领取 | 同阶段多次返还 | 存储 `lossAidUsedByPhase` 并在结算前检查 |
| 必遇 NPC 承诺失效 | 特定构筑永远遇不到对应 NPC | 只推迟普通阶段必遇，不影响殿试与终局承诺 |
| 教学变成替玩家做选择 | 玩家只点推荐项 | 推荐可忽略，展示理由而非唯一答案 |

### 回退原则

- 所有新行为受 `onboarding.enabled` 和配置权重控制；
- 出现严重问题时可先把入门卷总开关关闭，恢复标准选择器；
- 不删除新存档字段，旧版本读取时按未知字段忽略；
- 不通过强制推送或历史重置回退，优先从对应备份标签创建恢复分支。

---

## 十一、交付验收表

| 用户要求 | 文档位置 | 验收方式 |
|---|---|---|
| 首局难度降低 | 第四章 | 首场与晋阶试只抽合法难度角色 |
| 不改答题 | 第一章、第五章、第七章 | 答题文件快照与现有测试不变 |
| 便于执行 | 第五、六、七章 | 可直接按文件和 P0–P3 拆任务 |
| 保留失败与策略性 | 第四章 4.4 | 失败照常记录，只做一次轻补偿与复盘 |
| 不影响标准局 | 第四章、七章 | 标准局权重与胜率回归 |
| 可验证可回退 | 第八、十章 | 单变量试玩、总开关与备份标签 |

### 最终完成定义

只有同时满足以下条件，才可宣告该优化完成：

- P0–P2 已实现且对应自动化测试通过；
- 答题配置、逻辑和 UI 未发生变化；
- 本地与云端 NPC 配置一致；
- 标准局回归通过；
- 至少完成一轮新手真人试玩或明确标注“尚未完成真人验证”；
- 代码、配置和文档已独立提交；
- 已创建并推送不可复用的带注释备份标签。

---

## 十二、当前状态

### 已锁定

- 不修改任何答题相关内容；
- 采用首局专属 NPC 梯度；
- 保留真实失败；
- 首次败北提供一次轻量点拨；
- 首场论战采用分段信息展示；
- 入门卷结束后恢复标准规则。

### 仍开放

- 入门卷成绩是否进入非竞技本地榜；
- `standardWeight` 是否同步降低 advanced NPC 的出现率；
- 殿试是否需要独立的首局展示优化；本版默认不改难度。

### 下一步

按 P0 开始开发：先补 NPC 难度角色与权重，再实现入门池选择器，并只验证首场与首次秀才晋阶试的抽取结果。
