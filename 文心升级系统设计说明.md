# 飞花棋·文心升级系统设计说明

## 结论

这套升级系统的核心不是“文心越来越大”，而是让同一份灵感在三条路径间形成真实竞争：**追加骰（8灵感）/ 抽取新文心（8灵感）/ 强化已有文心**。升级默认发生在单局内，获得文心时为 Lv.1，等级随本局存档；图鉴可记录历史最高等级，但不提供跨局数值，以免把现有局内策略游戏改造成永久养成碾压。

> 本表所有数值均为首轮仿真起点 **[PLACEHOLDER]**，尚未经过接线后的 Monte Carlo 与实机测试。

## 品质与成本

| 品质 | 最大等级 | 单次升级成本 | 满级总投入 | 目标抽取概率 |
|---|---:|---|---:|---:|
| 普通 | 3 | 6 → 10 | 16 | 45% |
| 稀有 | 4 | 7 → 11 → 16 | 34 | 32% |
| 史诗 | 5 | 8 → 12 → 17 → 23 | 60 | 18% |
| 传说 | 6 | 9 → 13 → 18 → 24 → 31 | 95 | 5% |

### 成本依据

- 8 灵感是当前游戏的两个核心 sink：访胜抽签与追加骰。首级升级成本围绕 6–9 设置，保证它与“再赌一枚骰/再抽一枚文心”同桌竞争。
- 单次最高 31，小于灵感上限 48，理论可支付；传说满级 95 必须跨多个资源窗口，不能随手点满。
- 主动文心使用消耗最低为 1。史诗/传说后段可以从 2 降至 1，但不允许降至 0，否则升级会掏空灵感经济的 sink。

## 全量覆盖

- 文心数量：37（被动 30、主动 7）
- 品质分布：普通 8、稀有 14、史诗 11、传说 4
- 逐级明细：155 行
- 效果类型：21 类
- 实装验收：11 项（P0/P1/P2）

完整逐级数值见 `文心升级系统完整数值表.xlsx`：

1. `设计总览`：核心假设、设计支柱、失败信号、品质摘要
2. `品质成本曲线`：18 行成本曲线 + 折线图
3. `文心总表`：37 枚文心品质、旧版等效等级、Lv.1/满级效果
4. `逐级数值`：155 行完整参数，可直接转配置
5. `效果字段映射`：21 类 effect 的字段、单位、上下限与边界
6. `实装验收`：存档、引擎、UI、抽取与仿真验收点

## 必须先修的真实接线缺口

1. **T016「文思泉涌」**：配置是被动 `dice_mult`，但当前 `game.js` 只在主动文心循环读取 `dice_mult`，所以其 ×6 事实上不生效。实装升级前必须把被动倍率接入，并规定“主动倍率覆盖被动倍率”。
2. **T011「知人论世」**：配置是被动 `copy_affinity`，当前也只在主动循环读取。升级系统新增 `effect.ratio`（Lv.1 复制 55% → Lv.6 复制 130%），必须补被动接线。

## 关键实现边界

- 新增 `s.talentLevels: { [talentId]: level }`；存档版本升 v3，旧档默认迁移为 Lv.1。
- `attr_flat` 与 `start_insp` 升级只结算新旧等级差值；替换 `attr_flat` 文心时按当前等级完整撤销。
- 战斗开始后锁定文心等级快照，战斗过程中不可升级，避免计时与结算参数漂移。
- `crit` 多来源仍取最高倍率、不相乘；最高触发概率 47%。
- `copy_affinity` 只复制正相性，最高 130%；负相性不反向利用。
- 抽取建议先按品质 45%/32%/18%/5% 抽档，再在未拥有文心池中抽卡；流派初始文心不受品质概率影响。

## 首轮测试失败信号

- 普通文心平均升级率 <35%：首两级成本过高或反馈太弱。
- 史诗/传说满级率 >15%：灵感 sink 失效，高品质点满过于常态。
- 单一文心持有时升级选择率 >75%：出现无脑必点。
- 升级玩家相对同档未升级玩家胜率提升 >15pp：纵向成长压倒局内决策。
- 主动文心平均每场使用率 >70%：后续消耗过低；<15%：成本/收益不匹配。

## 建议下一步

按 Excel 的 `实装验收` 顺序推进：P0 数据与存档 → P0 引擎接线与升级交易 → P1 UI → P1 抽取分档 → P2 仿真。接线完成后至少进行 10 万局抽取分布验证、五策略 Monte Carlo，以及对主动文心使用率/普通升级率/史诗传说满级率的实机样本验证。
