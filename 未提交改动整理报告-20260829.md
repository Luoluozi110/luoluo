# 未提交改动整理报告（2026-08-29 晚）

> 任务：将工作区现有未提交改动全部整理出来，区分可交付与临时诊断文件，并与远端 main 对照。

---

## 一、仓库状态总览

| 项目 | 值 |
|---|---|
| 本地分支 | `codex/wenxin-dice-rework-20260824` |
| 本地 HEAD（工作树提交对象） | `b78e9e4` (docs: design sidequest-exclusive NPC roster) |
| 本地 vs 远端分支 | ahead 11 / behind 14（并行守护进程仍在推进） |
| 远端 main | `4c2a257` fix(editor): repair cloud bridge and version sync<br>（其父链含 `96f4b3b` 文档治理、`93bcf73` 三方合并部署） |
| 已暂存改动（index vs HEAD） | **90 文件，+13078 / -11617** |
| 未暂存改动（工作树 vs index） | **9 文件，+186 / -34** |
| 未跟踪文件/目录 | 15 项（含 2 个目录、若干 `_*` 临时文件） |

> ⚠️ 并发会话仍在线：并行守护进程会毫秒级把本地分支 ref 拉回旧基线，本地 commit 后 ref 会被回拉（commit 对象仍存于 reflog）。交付以 REST 部署提交 + 远端 backup 标签为权威，本地 ref 不可依赖。

---

## 二、改动清单（按「已发布 / 未发布」分类）

### 2.1 已发布到 main（工作树内容 = main 完全相同，无需再提交）— 约 46 项

这些改动工作树与 main 对应 blob 一致，属「已发布但本地尚未封存提交」：

- `README.md、overview.md`
- 编辑器种子/逻辑（已同步 main）：`feihua-editors/assets/js/seed-*.js`（affinity/board/copy/events/questions/affinity/sky/synergies/talent-upgrade/talents/sidequests）、`cloud.js`、`feihua-wenxin-synergy-gaps.md`、`feihuaqi-npc-mechanism-overall-summary.md`
- 游戏侧已发布：`feihuaqi-playable/config/{affinity,npcs,sidequest-npcs,sidequest-talents,sky,synergies,talents}.json`、`css/ui.css`、`index.html`、`js/engine/{album,game,save}.js`、`js/ui/{app,board,codex,modals}.js`、多数 tests
- 其它：`scripts/serve-editor-bridge.mjs`、`scripts/start-editor-bridge.mjs`、`feihua-editors/tests/*` 多数、`bridge-cors.integration.test.mjs`(untracked)

### 2.2 未发布（工作树 ≠ main，内容较 main 更新或有差异）— 17 项

| 文件 | 说明 |
|---|---|
| `feihua-content.json` | ⚠️ 详见下方「关键发现」 |
| `feihua-editors/assets/css/styles.css` | 样式调整 |
| `feihua-editors/assets/js/adventure.js` | 奇遇逻辑 |
| `feihua-editors/assets/js/common.js` | 版本守卫/发布逻辑（index 未暂存段为发布固定+回读校验） |
| `feihua-editors/assets/js/copy.js` | 文案 |
| `feihua-editors/assets/js/feihua-contract.js` | 编辑器契约（AM 未完全落入 main） |
| `feihua-editors/assets/js/richedit.js` | 富文本编辑器（A） |
| `feihua-editors/assets/js/talent.js` | 文心升级 |
| `feihua-editors/index.html` | 编辑器入口 |
| `feihua-editors/tests/config-contract-loader.test.mjs` | 契约加载测试 |
| `feihuaqi-playable/config/talent-upgrade.json` | 文心升级数值 |
| `feihuaqi-playable/js/engine/config-contract.js` / `config.js` / `rules.js` / `js/ui/battle.js` | 运行时 |
| `feihuaqi-playable/tests/extra-dice-pct.test.mjs` | 追加骰测试 |
| `feihuaqi-playable/tests/font-fallback.test.mjs` | 字体兜底测试（A） |

### 2.3 main 尚无此文件（本地新增，未发布）— 约 29 项

- 文档：`docs/灵感骰文心同步排查说明.md`、`docs/灵感骰文心改动方案.md`、`feihua-编辑器UI重建说明.md`、`feihuaqi-numeric-tuning.md`
- 脚本（可交付工具）：`scripts/{analyze_conflicts,classify_merge,fetch_base_and_classify,fetch_missing_files,merge_content_json,resolve_conflicts,three_way_merge,backup_deploy_preserve_editor,backup_editor_contract_fix,backup_editor_richedit,cleanup_debug_scripts,deploy_editor_full,deploy_wenxin_full,diag_editor_sync,diff_main_local,repair_mojibake,sync_all_from_config}.mjs`、`scripts/sync_desktop_content.mjs`
- 测试：`tests/deploy-source-stability.test.mjs`、`feihua-editors/tests/cloud-sync-integrity.test.mjs` 等
- 其它：`gen_talent_upgrade.py`、`启动编辑器.cmd`、`文心骰组联动改版说明.md`、`灵感与NPC平衡调整说明.md`

### 2.4 未跟踪临时/诊断文件（**应排除交付**，建议归档或清理）

| 路径 | 性质 |
|---|---|
| `_backup_before_sync_20260829/` | 同步前备份（15 个种子/配置文件） |
| `_merge_diff/`（≈110 文件） | 三方合并中间产物（base/main/local/conflict 快照） |
| `_live_main_feihua-content.json`(630KB) | 线上 main 内容抓取快照 |
| `_main_tree.json` / `_verify_tree.json` | 树结构诊断快照 |
| `_cs_index.html` / `_cs_talents.json` | CloudStudio 提取快照 |
| `feihua-editors/tests/_verify-richedit.mjs` | 调试脚本（此前线上曾要求清理同类 `_*`） |
| `scripts/_bump_editor_v/_fix_editor_load/_list_js/_repro_load/_tag_commit/_verify_v.mjs` | 调试脚本 |
| `feihua-editors/tests/bridge-cors.integration.test.mjs` | ⚠️ 非 `_*` 前缀，实为**正式集成测试**（可交付，需入库） |

---

## 三、⚠️ 关键发现：main 内容疑似再次回退

对比 `origin/main:feihua-content.json`（当前 main `_version:5`）与本地工作树 `feihua-content.json`（`_version:4`）：

| 板块 | MAIN (_v5) | 本地 (_v4) | 备注 |
|---|---|---|---|
| questions | 87 | 87 | 一致（77→87 题） |
| events | 42 | 42 | 一致 |
| **talents** | **61** | **49** | ⚠️ main=61（旧值），本地=49（正确值） |
| **sky** | **6** | **7** | ⚠️ main 缺 SK07「彩彻区明」 |
| **narrative** | **5** | **11** | ⚠️ main 丢失 6 键叙事 |
| npcs | 6 | 6 | 一致 |
| 康尔玉 id | `kang_er_yu` ✅ | `kang_er_yu` ✅ | |
| 康尔玉 `strictlyHigherThan` | `["shi","ci"]` ✅ | `["shi","ci"]` ✅ | 两边均完整（已复核 palace/stage 两处守卫） |
| `difficultyBoost`+`balanceVersion:2` | 10 处 ✅ | 10 处 ✅ | 两边一致 |

**结论**：main 的 `_version` 抬到了 5，但内容却是「61 文心 / 6 天象 / 5 叙事」——与 8-29 白天曾被另一会话桥接回退的旧缓存形态完全一致。**本地工作树（49 文心 / 7 天象含 SK07 / 11 叙事）才是正确版本**。若以当前工作树为主，后续部署提交必须把本地版 `feihua-content.json` 连同编辑端各 seed 一并发布，绝不能照搬 main 现在的旧值；发布后必须回读核验康尔玉守卫生效。

---

## 四、未暂存改动（9 文件）本质

工作树相对 index 的二次修改，内容为「版本守卫 + 发布桥接」增量：

- `feihua-editors/assets/js/cloud.js`：publishRepo/publishGist 均携 project、发布后回读逐模块校验（+36 行）
- `feihua-editors/assets/js/common.js`：`effectiveProjectVersion`、PROJECT_FIELDS 增 `sidequests/sidequest-npcs`、桥接状态文案细化、发布按钮 busy 态
- `feihua-editors/assets/js/feihua-contract.js`：契约加载（+58）
- `feihua-editors/index.html`（+6）
- 测试：`cloud-publish-verification`、`cloud-sync-integrity`、`config-contract-loader`、`gh-bridge`
- `scripts/serve-editor-bridge.mjs`：Gist 分支版本拒绝（+48）

> 行尾注意：这些文件 LF⇄CRLF 告警属正常（CloudStudio 换行差异），比对以 `?v=` token 与 blob SHA 为准。

---

## 五、下一步建议（供确认后执行）

1. **明确交付路径**：接受一份「整理现状 → 提交到本地分支 → 双平台部署」的整理提交，含 2.2/2.3 未发布改动 + 本地版 `feihua-content.json` + 编辑器 9 个未暂存文件；`bridge-cors.integration.test.mjs` 一并入库。
2. **处理临时文件**：`_merge_diff/`、`_backup_before_sync_20260829/`、`_live_main_*.json`、`_*_tree.json`、`_cs_*.{html,json}`、`scripts/_*.mjs`、`feihua-editors/tests/_verify-richedit.mjs` —— 归类为「诊断留档」，若需保留则移入 `_archive/` 或删除（不随交付提交）。
3. **先通测再部署**：全量测试（游戏 77 + 编辑器 17 + 工作区）→ bump `?v=` → 同步 `feihua-content.json` 采 `{...previous,...新块}` 保全部板块 → GitHub Pages（REST API）+ CloudStudio + backup 标签 → 线上核验康尔玉守卫生效与 SK07 存在。
4. **并发安全**：动仓库前确认无并行会话；部署提交不依赖本地 ref，以 REST commit + 远端标签为权威。