# 飞花棋 NPC 三机制 · 阶段 B 交付（交互反馈 UI）

> 阶段：B —— 研判卡 → 定策提示 → 结算明细 → 图鉴四级认知 → 殿试场间评语。
> 状态：已完成并通过自测与冒烟，等待阶段 B 大节点确认后进入阶段 C（配置 + 编辑器）。
> 覆盖章节：第四章「交互流程」的 UI 侧落地（引擎数据阶段 A 已就绪，本阶段做视觉/文案层）。

---

## 一、本阶段做了什么

| 子任务 | 内容 | 落地文件 |
|---|---|---|
| B1 接入勘察 | 确定 intentLocked / out.mech / getFoeCognition / palaceLayers 四类数据的 UI 消费点 | — |
| B2 研判卡 + 定策提示 | 战斗开场研判卡（行藏/立意/所长/所短）、选文体/选风格面板下方的破绽反制提示 | `js/ui/mechHints.js`（新）、`js/ui/battle.js` |
| B3 结算明细 | 算分动画后追加「招牌·破绽·修正」机制明细段 | `js/ui/battle.js` |
| B4 图鉴四级认知 + 殿试评语 | 图鉴卡认知徽标、对手详情认知进度、王侍郎殿试场间评语 | `js/ui/codex.js`、`js/ui/battle.js`、`js/engine/game.js` |
| 样式 | 研判卡/提示/明细/认知/评语全套古风样式 | `css/ui.css` |

---

## 二、实现要点

### 1. 文案单一来源（关键架构决策）
新增 **`js/ui/mechHints.js`**：把所有机制数据→人类可读文案的翻译集中成纯函数
（`intentHint / weaknessHint / settleLines / signatureName / weaknessName`）。
- **不触碰任何算分逻辑**；配置改措辞即改 UI，无二次硬编码漂移；
- 可被 Node 单元测试直接消费（本阶段 91 + 11 项断言）。

### 2. 研判卡分组披露
`intentHint` 按 `intentLocked.styleDisclosed / mannerDisclosed` 决定能看到多少：
- 教学型（周小满）明示「拟用诗体」；
- 文风立意型（宇文渊）只给「重哲理/沉郁」方向；
- 未披露（王侍郎）只给身份级「成于招牌 / 露于破绽」。

### 3. 定策期破绽提示
`weaknessHint` 根据 `weakness.template` 逐类生成古风反制建议
（换体/换策/只用基础骰/压卷/相得文风等 8 类），出现在选文体与选风格面板底部。

### 4. 结算明细三段式
`settleLines` 把 `out.mech` 译为「招牌被压制与否 → 破绽本场状态 → 修正生效明细」，
在算分动画后追加为单独的 `.mech-result` 段，玩家可看清每一条修正来源。

### 5. 图鉴四级认知
- 卡面徽标：`识·未识/相识/察意/破招`（`cog-badge lv0..3`）；
- 详情浮层：四格进度轨 + 交手/破招统计 + 等级释义。
- 稳定 id 映射：机制 NPC 用具名 id，普通 NPC 用档位 id（`foeId(tier,npc)` 辅助）。

### 6. 殿试场间评语
`palaceVerdict` 仅在机制主考官（王侍郎，`sig_palace_adapt`）且 `session.isPalace`
时出评语；随 `palaceLayers`（0/1/2）与胜负给出不同叙事。
`game.js` 在 session 上暴露 `palaceLayers`，并抽出 `stableFoeId` 统一稳定标识。

---

## 三、验证结果

| 层 | 脚本 | 结果 |
|---|---|---|
| 单元·文案 | `sim_mech_hints_unit.mjs` | 7 名机制 NPC + 边界：**91/91 通过** |
| UI·DOM 冒烟 | `sim_mech_hints_ui.mjs`（jsdom） | 研判卡/提示/明细/评语落 DOM：**11/11 通过** |
| 阶段 A 回归 | `sim_npc_mech_unit/engine/health.mjs` | 26/26、11/11、**PASS**（未破坏） |
| 服务冒烟 | 本地 8199 起服 | 全部模块/config 200，可玩性完整 |

---

## 四、玩家可见变化一览

- 遭遇机制 NPC 时，开场短暂浮现「研判卡」（其行藏与长短）。
- 选文体/选风格时，面板底部出现「临题有人言…」的破绽反制提示。
- 算分后多出一段「机制结算」：招牌被压制与否、破绽命中、修正生效逐条可读。
- 图鉴阁：已交手 NPC 带认知徽标；点开详情可见四级认知进度与破招次数。
- 殿试对王侍郎：每场后出「场间评语」，体现跨场适应的叙事。
- 普通 NPC：以上全部**不出现**——行为与旧版完全一致。

---

## 五、本阶段边界（未包含 vs 后续阶段）

- **阶段 C（配置 + 编辑器）**：其余 NPC 正式机制配置文案、编辑器 npc.js/seed-npcs.js 支持 `mech` 字段、云端同步。
- **阶段 D（异常兼容）**：E0~E4 完整化（意图锁定保护、幂等、破绽不可达回退、副招牌上限、存档降级）。
- **阶段 E（仿真校准 + 部署）**：`sim_npc_mechanism.mjs` 全量校准、封笔/胜率/招牌摆幅调参、GitHub Pages + CloudStudio 上线。

---

## 六、待确认 / 遗留

1. 请确认**阶段 B 通过**，放行进入阶段 C（配置内容 + 编辑器支持）。
2. 研判卡为「短暂浮现」式，若希望常驻在指定面板可再调（当前为体验节奏优先）。
3. `weaknessHint` 中「文风组合破绽」有一处 `{{retention 分}}` 占位未替换（模板未提供具体值时的兜底），阶段 C 配置 NPC 时补齐。
4. 本地 8199 服务器已启动，可直接打开预览联调；关闭后用 `kill` 停掉即可。
