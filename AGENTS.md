# 项目协作与版本备份规则

## GitHub 版本备份

- 每次完成代码、配置、内容或文档更新后，必须将本次更新作为独立 Git 提交推送到已配置的 GitHub 远端，之后才可宣告交付完成。
- 每次更新提交必须创建不可复用的带注释标签，格式为 `backup/YYYYMMDD-HHmm-简短主题`，并将该标签推送到 GitHub，作为明确的回退点。
- 禁止覆盖、移动或删除既有备份标签；需要修正时创建新的提交和新标签。
- 提交前应运行与改动范围相称的验证；提交信息应说明本次更新的实际内容，不得把无关改动混入同一备份。
- 最终交付说明必须列出 GitHub 提交 SHA、备份标签和验证结果。
- 如果当前目录尚未初始化 Git、没有 GitHub 远端、认证不可用或推送失败，必须明确说明“GitHub 备份尚未完成”，不得声称本次更新已经完成备份。
- 回退时优先从备份标签创建恢复分支；除非用户明确要求，不得对共享分支执行强制推送或破坏历史的重置。
