# 飞花棋小程序架构设计 · 交付总览

## 本次做了什么

为「文心棋（飞花棋）」设计了微信小程序架构，选型为**原生小程序 + 微信云开发**，
并产出了可直接导入微信开发者工具的工程骨架。

## 核心判断

这是一次「引擎复用、UI 重写、资源外迁」的移植，而非从零开发：

- `js/engine`（496KB，纯逻辑无 DOM）**可原样复用**，只需改模块语法与配置读取方式
- `js/ui`（484KB，DOM 拼装）**全部作废**，需重写为 WXML/WXSS，约占总工时六成
- 22MB 素材**必须挤出包体**，音乐 9.8MB、字体 4.1MB 走云存储按需加载
- 66 处 `localStorage` 调用用同步适配器全局注入，引擎零改动

## 三个关键结论

1. **主包配置实测 378.8KB**，加引擎核心与页面代码后约 930KB，距 2MB 红线有充分余量。
   配置由 `scripts/sync-config.mjs` 按「启动链路是否读得到」自动拆分。
2. **小程序前端不能直连 Supabase**——`supabase.co` 无 ICP 备案，无法加入 request 合法域名。
   双端榜单统一只能走云函数出网中转，已在 `submitScore` 中实现该分支。
3. **资质是唯一的卡死风险**：个人主体不可选小程序「游戏」类目。
   建议以教育 / 文化类目（国学诗词）定位上线，并在动工第一周确认口径。

## 交付物

| 文件 | 说明 |
|---|---|
| `feihuaqi-miniprogram/ARCHITECTURE.md` | 完整架构设计文档（十节，含合规与里程碑） |
| `feihuaqi-miniprogram/` | 工程骨架，可导入开发者工具 |
| `feihuaqi-miniprogram/scripts/sync-config.mjs` | 配置拆分同步脚本，已实测运行 |

（初版用于界面预览的 `utils/engine-host.js` mock 已被 `engine-runtime.js` 取代）

### 本轮新增

| 文件 | 说明 |
|---|---|
| `feihuaqi-miniprogram/ARCHITECTURE.md` | 已更新，新增「第一周闭环」实装记录 |
| `utils/engine-runtime.js` | 引擎装配层：startGame / playTurn / project |
| `utils/ui-adapter.js` | 把引擎回调转成事件流的适配器 |
| `scripts/sync-config.mjs`、`build-engine.mjs` | 配置拆分与引擎搬运，均已实测运行 |
| `test/` | 语法核验 + 两套无头自检 |

---

# 第一周闭环 · 已完成

## 进展

「选流派 → 对局 → 结算」已在真实引擎上跑通，不是 mock。

- **引擎搬入**：保留 ES Module 语法，仅剥离 import 路径上的 `?v=` 缓存戳（全工程 5 处，脚本自动处理）
- **配置编译**：`config/*.json` 编译成 `engine/embed-config.js`，取代 `fetch`；原 JSON 已排除出包体
- **驱动模型**：`game.start()` 后循环 `await game.playTurn()`，UI 通过回调被动响应
- **战斗降级**：不必先做六步对决 UI，直接调用引擎自带的 `session.resolve(style, manner, dice)`

## 验证结果

| 项目 | 结果 |
|---|---|
| 全量语法核验 | 35/35 通过（ESM 走 `.mjs` 镜像） |
| 引擎自检 | 5/5 局走到结算，覆盖「殿试已毕」与「灵感封笔」两种结局 |
| 装配层自检 | 走页面实际调用的那一层，总分与段位正常产出 |
| 线上核验 | blob 505 → 530，他方文件完整保留，测试镜像未入库 |

主包约 0.9MB（引擎 788KB + 页面与工具约 100KB）。

## 第一周遗留限制

- **读档未实装**，主菜单「继续游戏」暂提示即将开放
- **装配屏未实装**，开局由选流派直接进入对局

---

# 第二周真实交互 · 已实装

## 机制：挂起通道

玩家决策的难点在于引擎 `await` 一个来自人的返回值。做法是让适配器发 `request` 事件并挂起 Promise，页面渲染对话框、玩家操作后调用 `evt.resolve(value)`，引擎才继续。

关键约定：sink 收到 request 时返回 `true` 表示接管，否则适配器回退自动应答——**界面没做完的部分不会把流程卡死**，可以边做边接。

另一条纪律：引擎原始对象（如战斗 session）通过事件的 `raw` 字段传，页面挂在实例上而**不进 `data`**，它不可序列化。

## 已接好的界面

| 界面 | 玩家看到的 |
|---|---|
| 答题 | 情境 + 题干 + 选项，落笔作答 |
| 奇遇 | 事件描述 + 选项，就此决定 |
| 天象 | 天象说明 |
| 博闻抉择 | 专攻一体 / 以学驭战 / 兼收并蓄 |
| 文心替换 | 新得文心与已有文心，定夺（不选即放弃） |
| 战斗台 | 六步：遭遇 → 选文体 → 选文风 → 掷灵感骰（可追加）→ 算分对决 |

战斗台每步读引擎实时数据：`styleScore` 给文体底分、`affinityOf` 给文风相性、`previewDiceScore` 预览骰面、`extraDiceCost` 算追加消耗，追加走 `session.spendExtraDice(cost)`，灵感不足则拒绝。

## 验证

| 项目 | 结果 |
|---|---|
| 语法核验 | 35/35 通过 |
| 兜底链路（无界面） | 3/3 局走完结算 |
| 真实交互 | 三局玩家决策 38 / 49 / 40 次，**兜底均为 0** |

**策略确实生效的佐证**：自动应答时战绩常是「0 胜 15 负」，改为挑最高相性文风后变成「5 胜 5 平 8 负」。

---

# 第三周：兜底清零

原先靠自动应答的界面已全部接好：

| 界面 | 玩家看到的 |
|---|---|
| 名胜 | 访胜抽签 / 入世另行 / 行路凝心 / 翻看手记 / 离开，五个去向 |
| 三心择一 | 三枚未拥有文心择一，`-1` 为放弃 |
| 晋阶 | 阶段推进说明，并为本章择一「开卷句」 |
| 殿试开场 | 本场题目与开卷句选择 |

现在整局游戏中**没有任何一步是机器替你做的**。

> 一处限制：支线内容（`sidequests`）在 `pkg-side` 分包，主包配置未含，
> 所以「入世另行」按钮虽在，实际入口仍受配置限制。

## 接下来的优先级

1. **分包配置加载**：图鉴与支线配置在分包，需在分包入口注册后合并进 cfg
2. **装配屏**：正式开局链是「选流派 → 文心配置 → 名号 → 开局」，目前跳过了装配与名号
3. **读档**、**云开发联调**、**排行榜页**

## 下一步

1. 确认小程序主体与类目口径（资质问题，越早越好）
2. 导入工程，替换真实 AppID，开启云环境跑一次真机
3. 按上面的优先级推进
